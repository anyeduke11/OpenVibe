# OpenVibe P1.1 · T10（CLI `clean` 受管文件退场 + 预览体量与 token 估算）实现计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 交付 P1.1 · T10 的两件产品能力——`openvibe clean`（sync 的反向半边，只退场「确实由我方写入且未被改动」的受管文件）与标准包预览的体量/近似上下文占用估算（`sizeEstimate`），并把规格与实况的三处冲突在动码前定稿。

**Architecture:** 沿用仓库既有的「薄客户端」分层（design §9）：判定逻辑全部落在 `packages/core/src/inject/`（纯函数、零副作用，因此 dry-run 的「零写入」是结构性事实而不是调用方记得加分支），`apps/cli` 只做交互、文件 IO 与渲染。估算侧 `estimateTokens` 是 core 纯函数，`sizeEstimate` 的组装点按 m6a FR-6.6 钉在 `apps/server/src/lib/pack-assemble.ts:toPreviewOut`——**不进 manifest、不进 `directoryFiles()`**，这是「属 B 级不属 A 级」的物理凭据。

**Tech Stack:** TypeScript + pnpm workspace（`apps/cli` `apps/server` `apps/web` `packages/{core,shared,adapters}`）、commander、@clack/prompts、Fastify、zod、vitest（三 project：`unit` / `integration` / `cli`）、esbuild 单文件 CLI、React 18 + Tailwind。

**Spec:** [docs/specs/m6-cli-injection.md](../../specs/m6-cli-injection.md) v1.2（FR-6 + §6.10 + 验收 §7.10 a–j）与 [docs/specs/m6-standard-pack.md](../../specs/m6-standard-pack.md) v1.1（FR-6 + §5 + 验收 8–12）。任务清单：[docs/tasks.md](../../tasks.md) §2b·T10。范围裁定正文：[docs/dev-plan.md](../../dev-plan.md) §15。

---

## Global Constraints

逐条抄自规格，对每个任务都生效：

- **A 级冻结契约不得触碰**：`tests/golden/` 一个字节都不许改。收口凭据是 `git diff --name-only tests/golden/` **输出为空**（m6a 验收 12），不是叙述句。
- **零新依赖**（m6a FR-6.5）：不引入任何 tokenizer。Web 入口现 291.02 kB / 预算 300 kB / 余量 3%，新代码不得让 `pnpm bundle:check` 转红。
- **删除凭据 = `managed === true` 且磁盘 sha256 == lock 期望**（m6b FR-6.3）。`managed !== true` 一律 `FOREIGN`，**永不删除**；`.openvibe/` 自身也归 `FOREIGN`。
- **删除前一律备份**（FR-6.4）：`copyFileSync` 进 `<project>/.openvibe/backup/<UTC时间戳>/`，保留相对路径，同戳追加 `-1`/`-2`；备份目录规则与 sync 同源（复用 `apps/cli/src/commands/sync.ts` 已导出的 `utcStamp` / `uniqueBackupRoot`）。
- **信任根净化**（FR-6.6）：lock 可被手改，删除前对每条 `files[].path` 走 design §7.7 净化；任一违规 → **整包拒绝**、退出码 1、报告违规项、**合法条目也不删**。
- **并发互斥**（FR-6.8 承 §6.9）：真删盘前抢同一把 `O_EXCL` `<project>/.openvibe/sync.lock`，`command` 字段写 `clean`；抢不到 → 退出码 1 + `SYNC_BUSY` + 零删除；`--dry-run` 不抢锁也不被挡。
- **退出码**（FR-6.9）：`0` 全清 / `2` 存在被保留的 `DRIFT` / `1` 错误。
- **措辞闸**（m6a FR-6.2）：`1.2` 与 `4` 是粗估常数，展示层一律带 `≈`，且只能写「近似上下文量（非计费口径）」，**不得出现「token 数」这类断言式表达**。
- **阈值只提示不阻断**（FR-6.4）：`approxTokens > 12000` 仅置 `warn=true`；导出与 `sync` 的结果与 `warn` 无关，且**首启预置包自己就会亮黄条**——不得为消掉它而调高阈值。
- **不改 `.gitignore`、不删 `.openvibe/` 目录本身**（FR-6.5 收尾只提示）。
- **commit / push 需 owner 放行**（本仓长期约定，dev-plan §0.4 工作节奏）。本计划每个任务末尾的「交棒」步骤是**把变更清单与验证输出交给 owner 等待放行**，不是自行 `git commit`。
- **测试夹具与 ID 口径**：CLI 验收支次固定为 `CLI-CLEAN-01..10` ↔ m6b §7.10 a–j；core 估算用 `CORE-SIZE-01..05`；server 估算用 `SRV-EST-01..03`。
- 本机 node 是 v26.4.0 而 CI 是 22；新测试不得依赖 node 26 独有 API。

---

## 先读：三处「规格写的」与「代码做的」不一致（Task 1 的输入）

这三条是本轮读码 + 实跑撞出来的，**都必须先定稿再动码**，否则 Task 5/7 会照着不可能的断言去实现。前两条各有实测/常量级证据，第三条是措辞冲突。

> **✅ 2026-09-24 已定稿（Task 1 完成，纯文档，未动码）**：三条按下文裁定落入 `docs/specs/m6-cli-injection.md` v1.3 与 `docs/specs/m6-standard-pack.md` v1.2，并顺带修了**同源涟漪三处**（原计划没列，属同一根因）：
> ①-涟漪 → m6b FR-6.3 的 `managed` 引文块：原句把「上一次全量 sync 留下、本次 `--target` 重刷后降为 `managed:false`」当成可能状态，与 ① 同一个错；已改为「`managed` 只升不降，`false` 只出现在首次注入即被过滤的文件上」。
> ②-涟漪 → m6b FR-6.3 四态表的 `FOREIGN` 行：原文把「上一包遗留的孤儿文件」列为 FOREIGN，但 ③ 的合并语义下它们仍带 `managed` 登记位、按磁盘比对落进 `IN_SYNC`/`DRIFT`/`ABSENT`；已改为「根本不在 lock 里的磁盘文件」（clean 只走 lock 登记项），并显式声明换包旧文件不算这一类。
> ③-涟漪 → m6b §6.2：「MVP 不清理」补成「不清理只约束 sync，`clean` 会一并退场」，避免下一轮又长成一条相反断言。
> 另补一处**规格空白**（写 Task 3 测试时发现）：FR-6.2 与 §7.10 f 只规定了「无 lock」，没规定「lock 存在但读不懂」；已加 FR-6.2 后半句与验收 **f2**（`LOCK_INVALID`、零删除、不建 `backup/`），对应计划里的 `CLI-CLEAN-06c`。
> 以下三条正文保留原样，作为 owner 复核（待办 #7）与 DEV-0026 的证据材料。

**① m6b §7.10 j 的构造在现行 lock 语义下产不出 `managed:false`。**
实测（`sync --file` 全量注入 `targets:['claude-code','cursor']` 后再 `sync --target cursor` 重刷，读回 `pack.lock.json`）：

```
after full sync      : .cursor/rules/openvibe.mdc=true CHECKLIST.md=true CLAUDE.md=true TERMS.md=true
after --target cursor: .cursor/rules/openvibe.mdc=true CHECKLIST.md=true CLAUDE.md=true TERMS.md=true
fresh --target cursor: .cursor/rules/openvibe.mdc=true CHECKLIST.md=true CLAUDE.md=false TERMS.md=true
```

原因在 `packages/core/src/inject/lock.ts:36`——`managed = 本次实写 ∪ 上次已受管`，而该并集是**刻意**的（同文件 :19-25 的注释：若按「本次是否写入」重算，一次 `--target` 之后其余文件会在下次全量 sync 时从 DRIFT 降级成 CONFLICT，用户手改过的包内文件会被当成陌生本地文件，那是信息丢失）。所以 §7.10 j 的「先全量 sync，再 `--target` 重刷 ⇒ `managed` 降为 false」不成立，照抄会做出一条永远失败的断言。
**裁定（本计划按此实现）**：j 的**判据不动**（`managed:false` 不构成删除凭据），只改**构造**为第三行那种——全新项目 + 用户自己的 `CLAUDE.md` 逐字节等于包内容 + 首次即 `sync --target cursor` ⇒ lock 里 `CLAUDE.md` 期望哈希在、`managed:false`。这正是 FR-6.3 括号里自己举的例子（「用户自己的同名文件恰好与包内容逐字节相同」），改后与规格正文一致、与实现一致。规格侧按 §0.3 **C 级**（只修验收构造，不改行为）在 m6b 版本行登记。

**② m6a §7 验收 9 的「`perTarget[].adapter` 集合 == `coveredPlatforms`」在语义上不可满足。**
`coveredPlatforms` 返回的是**兼容矩阵里的平台展示名**，且矩阵全部行的 `reads` 都是 `AGENTS.md`（`packages/adapters/src/compat.ts:12-48`），函数注释明写这是「本包**额外**覆盖平台」（`packages/adapters/src/registry.ts:65`）。实测：

```
claude-code+cursor -> []
generic-agents     -> ["Cline","Codex","Kimi Code","OpenCode","Qwen Code","Trae CN","zcode"]
```

一个 `targets:['claude-code','cursor']` 的包必须给出 2 条 perTarget 行（它有两个主文件），而它的 `coveredPlatforms` 是 `[]`——两条断言不可能同时成立。
**裁定**：perTarget 的键**取 `rendered.manifest.targets`**（adapter id 集合，稳定、确定序、与「该平台实际读进上下文的量」同构）；验收 9 前半句改为「`perTarget[].adapter` 集合与 `manifest.targets` 集合完全一致（与 `coveredPlatforms` 无关，后者只来自兼容矩阵）」。这属 §0.3 **B 级**（行为规格），在 m6a 版本行登记并说明改因。

**③ m6b §6.10「换包 ⇒ lock 整体重写、旧包独有文件脱离登记」与 `lock.ts:39-41` 相反。**
现行实现在两种情况下都**保留**旧 lock 里不在本次 plan 中的条目（含其 `managed` 位），依据是 §6.2「pack 更新后消失的旧文件 MVP 不清理，lock 保留其哈希备查」。
**裁定**：实现不动，§6.10 的措辞按实况修正为「换包后旧包独有文件**仍在 lock 中备查**，但新包不再写它们；`clean` 会把它们一并退场（因为它们确实由我方写入）」。**这条修正顺带说明了对 `clean` 的影响**：§6.10 与 §7.10j 都属 m6b v1.2 新增文本，owner 复核（待办第 6 项）尚未走过这处，改完要一并回报。

---

### Task 1: 三处规格冲突定稿（文档，B/C 级；不动码）— ✅ 已完成 2026-09-24，落地见「先读」节的记录

**Files:**
- Modify: `docs/specs/m6-cli-injection.md`（§6.10 措辞、§7.10 j 构造、版本行）
- Modify: `docs/specs/m6-standard-pack.md`（§7 验收 9、FR-6.3 的 `perTarget` 说明行、版本行）

**Interfaces:**
- Consumes: 上文三条证据
- Produces: 后续任务照已修正的规格实现；owner 复核材料（待办第 6 项）

- [x] **Step 1: 改 m6b §7.10 j 的构造**

把 `j.` 那支的构造句从「先全量 `sync`（写出 `CLAUDE.md` 与 `.cursor/rules/openvibe.mdc`），再 `sync --target cursor` 重刷」改为：

> j. **`managed:false` 不构成删除凭据**（FR-6.3 的那道闸）：临时项目内**先由用户自己写入** `CLAUDE.md`，内容取自包的 `CLAUDE.md` 逐字节（模拟「用户自己的同名文件恰好与包内容逐字节相同」），随后**首次**注入即 `sync --target cursor`（lock 里 `CLAUDE.md` 期望哈希在、`managed:false`，实测口径见 `lock.ts:36` 的「本次实写 ∪ 上次已受管」）→ `clean --yes` 后：`.cursor/rules/openvibe.mdc` 消失并进备份，**`CLAUDE.md` 逐字节原样留在盘上**、report 标 `FOREIGN`、退出码 0（无 DRIFT 残留），且备份目录内**不得出现 `CLAUDE.md`**。

- [x] **Step 2: 改 m6b §6.10 的最后一句**

将「lock 整体重写为新包内容，旧包独有而新包不含的文件自此脱离登记，既不是 `IN_SYNC` 也不是 `DRIFT`，按 §6.2 属不清理项」修正为「lock **合并**：旧包独有而新包不含的文件仍在 lock 中备查（§6.2 同一机制，实现见 `packages/core/src/inject/lock.ts:39`），新包不再写它们；`clean` 视其为 `managed` 登记项并一并退场——它们确实由我方写入且未被改动，留着反而使「零锁定」不成立。用户手工删过的则记 `ABSENT`。」

- [x] **Step 3: 改 m6a §7 验收 9**

```
9. preview 的 `sizeEstimate.perTarget[]` 中 `adapter` 集合与 **`manifest.targets` 集合**完全一致
   （**不是** `coveredPlatforms`：后者只来自兼容矩阵且全是 `AGENTS.md` 的行，
   `targets:['claude-code','cursor']` 时为空——见 `packages/adapters/src/registry.ts:65` 与 compat.ts。
   2026-09-24 按 §0.3 B 级修正，原句为「与响应既有 coveredPlatforms 完全一致」，该断言不可满足）；
   `footprint.files[].path` 集合与主字段 `files[]` 集合完全相等（不多不少，含合成的 `openvibe.pack.json`）。
```

同处补一句口径：`perTarget[].approxTokens` 由该平台读入的**文件拼接后整体估算**（单次 `ceil`），不是逐文件 `approxTokens` 相加（逐文件相加会系统性高估，两者差值随文件数增长）。

- [x] **Step 4: 两份规格的版本行登记**

m6b：`→ v1.3（2026-09-24，T10 开工前自审：§7.10 j 构造按实测改写、§6.10 合并语义修正。C 级）`；m6a：`→ v1.2（2026-09-24，T10 开工前自审：§7 验收 9 的 perTarget 键集合改指 manifest.targets + 补拼接估算口径。B 级）`。

- [x] **Step 5: 交棒**

`git diff --stat docs/specs/` 输出 + 三条证据原文（含那两行实跑结果）交给 owner，等放行。**不 commit。**

---

### Task 2: core 退休分类器 `planRetirement`（纯函数）

**Files:**
- Create: `packages/core/src/inject/retirement.ts`
- Create: `packages/core/src/inject/retirement.test.ts`
- Modify: `packages/core/src/inject/index.ts`（末尾加一段 export）
- Modify: `packages/core/src/index.ts:86` 那段 `from './inject'` 的具名导出（加新符号）

**Interfaces:**
- Consumes: `PackLock`（`@openvibe/shared`）、`fileSha256`（`../pack/fingerprint`）
- Produces: `RETIREMENT_STATES` / `RetirementState` / `RetirementAction` / `RetirementFile` / `RetirementPlan` / `RetirementInput` / `planRetirement` —— Task 3 的 `cleanAction` 唯一判定入口

- [ ] **Step 1: 写失败测试**

创建 `packages/core/src/inject/retirement.test.ts`：

```ts
import { describe, expect, it } from 'vitest'
import { fileSha256 } from '../pack/fingerprint'
import { planRetirement, RETIREMENT_STATES } from './retirement'
import type { PackLock } from '@openvibe/shared'

const lockOf = (entries: [string, string, boolean][]): PackLock => ({
  schemaVersion: 1,
  pack: { id: 'pk_x', name: 'x', version: '1.0.0', fingerprint: 'f' },
  injectedAt: '2026-09-24T00:00:00Z',
  files: entries.map(([path, content, managed]) => ({ path, sha256: fileSha256(content), managed })),
})

const disk = (map: Record<string, string>) => (rel: string): string | null => map[rel] ?? null

describe('planRetirement（m6b FR-6.3 三态 + FOREIGN）', () => {
  it('RT-01: counts 恒含四键且顺序固定，计划按路径码点序可重复', () => {
    const lock = lockOf([
      ['TERMS.md', 't\n', true],
      ['AGENTS.md', 'a\n', true],
    ])
    const p1 = planRetirement({ lock, readDisk: disk({ 'AGENTS.md': 'a\n', 'TERMS.md': 't\n' }) })
    expect(Object.keys(p1.counts)).toEqual([...RETIREMENT_STATES])
    expect(p1.files.map((f) => f.path)).toEqual(['AGENTS.md', 'TERMS.md'])
    expect(planRetirement({ lock, readDisk: disk({ 'TERMS.md': 't\n', 'AGENTS.md': 'a\n' }) })).toEqual(p1)
  })

  it('RT-02: managed 且磁盘==期望 → IN_SYNC + delete', () => {
    const lock = lockOf([['AGENTS.md', 'a\n', true]])
    const p = planRetirement({ lock, readDisk: disk({ 'AGENTS.md': 'a\n' }) })
    expect(p.files[0]).toMatchObject({ state: 'IN_SYNC', action: 'delete', managed: true })
    expect(p.removals).toEqual(['AGENTS.md'])
  })

  it('RT-03: managed 但磁盘被改 → DRIFT，默认 keep；force 才 delete', () => {
    const lock = lockOf([['AGENTS.md', 'a\n', true]])
    const kept = planRetirement({ lock, readDisk: disk({ 'AGENTS.md': '用户手改\n' }) })
    expect(kept.files[0]).toMatchObject({ state: 'DRIFT', action: 'keep' })
    expect(kept.removals).toEqual([])
    const forced = planRetirement({ lock, readDisk: disk({ 'AGENTS.md': '用户手改\n' }), force: true })
    expect(forced.files[0].action).toBe('delete')
    expect(forced.removals).toEqual(['AGENTS.md'])
  })

  it('RT-04: 磁盘不存在一律 ABSENT（managed 任意），不动盘', () => {
    const lock = lockOf([['A.md', 'a', true], ['B.md', 'b', false]])
    const p = planRetirement({ lock, readDisk: disk({}) })
    expect(p.files.map((f) => [f.state, f.action])).toEqual([['ABSENT', 'none'], ['ABSENT', 'none']])
  })

  it('RT-05: managed!==true ⇒ FOREIGN，即使磁盘与期望逐字节相同且 force=true 也不删', () => {
    const lock = lockOf([['CLAUDE.md', 'c\n', false]])
    const input = { lock, readDisk: disk({ 'CLAUDE.md': 'c\n' }) }
    expect(planRetirement(input).files[0].state).toBe('FOREIGN')
    expect(planRetirement({ ...input, force: true }).removals).toEqual([])
  })

  it('RT-06: .openvibe/ 下的登记项即使 managed=true 也记 FOREIGN（§6.3 教条）', () => {
    const lock = lockOf([['.openvibe/backup/2026T/CLAUDE.md', 'x', true]])
    const p = planRetirement({ lock, readDisk: disk({ '.openvibe/backup/2026T/CLAUDE.md': 'x' }), force: true })
    expect(p.files[0].state).toBe('FOREIGN')
    expect(p.removals).toEqual([])
  })

  it('RT-07: lock 缺 managed 字段（被手删）按未受管处理，安全侧', () => {
    const lock = { ...lockOf([['AGENTS.md', 'a\n', true]]), files: [{ path: 'AGENTS.md', sha256: fileSha256('a\n') }] } as unknown as PackLock
    expect(planRetirement({ lock, readDisk: disk({ 'AGENTS.md': 'a\n' }) }).files[0].state).toBe('FOREIGN')
  })

  it('RT-08: readDisk 抛错原样上抛（不把读失败当成「已自行退场」）', () => {
    const lock = lockOf([['AGENTS.md', 'a\n', true]])
    expect(() => planRetirement({ lock, readDisk: () => { throw new Error('EIO') } })).toThrow('EIO')
  })
})
```

- [ ] **Step 2: 跑到失败**

Run: `npx vitest run --project unit packages/core/src/inject/retirement.test.ts`
Expected: FAIL —`Cannot find module './retirement'`

- [ ] **Step 3: 实现**

创建 `packages/core/src/inject/retirement.ts`：

```ts
// packages/core/src/inject/retirement.ts —— clean 的三态分类器（m6b FR-6.3）。
// 纯函数：判据与 sync 的规划器同一条（磁盘 sha256 vs lock 期望哈希），不另造第二套词——
// 两套分类器必然在边界处分叉。删除动作留给 CLI，这里只出「谁可以删」。
import { compareCodeUnit, type PackLock } from '@openvibe/shared'
import { fileSha256 } from '../pack/fingerprint'

export const RETIREMENT_STATES = ['IN_SYNC', 'DRIFT', 'ABSENT', 'FOREIGN'] as const
export type RetirementState = (typeof RETIREMENT_STATES)[number]

/** delete=备份后删除；keep=DRIFT 被保留（--force 会变成 delete）；none=不动盘 */
export type RetirementAction = 'delete' | 'keep' | 'none'

export interface RetirementFile {
  path: string
  state: RetirementState
  action: RetirementAction
  lockSha256: string
  diskSha256: string | null
  managed: boolean
}

export interface RetirementPlan {
  files: RetirementFile[]
  counts: Record<RetirementState, number>
  /** 本次真要删的路径（IN_SYNC ∪ --force 的 DRIFT），码点序 */
  removals: string[]
}

export interface RetirementInput {
  lock: PackLock
  /** null = 磁盘不存在；抛错 = 读取失败（调用方按 §6.5 中止，不得当成 ABSENT） */
  readDisk: (relPath: string) => string | null
  force?: boolean
}

/** 项目根下第一段是 .openvibe 即非包产物：lock 可被手改，这条不能依赖 lock 的自觉 */
function isOpenVibeInternal(relPath: string): boolean {
  const [first] = relPath.split(/[\\/]+/)
  return first === '.openvibe'
}

export function planRetirement(input: RetirementInput): RetirementPlan {
  const force = input.force === true
  const files: RetirementFile[] = [...input.lock.files]
    .sort((a, b) => compareCodeUnit(a.path, b.path))
    .map((entry) => {
      const managed = entry.managed === true
      const content = input.readDisk(entry.path)
      const diskSha256 = content === null ? null : fileSha256(content)

      let state: RetirementState
      if (content === null) state = 'ABSENT'
      else if (!managed || isOpenVibeInternal(entry.path)) state = 'FOREIGN'
      else state = diskSha256 === entry.sha256 ? 'IN_SYNC' : 'DRIFT'

      const action: RetirementAction =
        state === 'IN_SYNC' ? 'delete' : state === 'DRIFT' ? (force ? 'delete' : 'keep') : 'none'
      return { path: entry.path, state, action, lockSha256: entry.sha256, diskSha256, managed }
    })

  const counts = Object.fromEntries(RETIREMENT_STATES.map((s) => [s, 0])) as Record<
    RetirementState,
    number
  >
  for (const file of files) counts[file.state] += 1

  return {
    files,
    counts,
    removals: files.filter((f) => f.action === 'delete').map((f) => f.path),
  }
}
```

- [ ] **Step 4: 出口接线**

`packages/core/src/inject/index.ts` 末尾追加：

```ts
export {
  RETIREMENT_STATES,
  planRetirement,
  type RetirementAction,
  type RetirementFile,
  type RetirementInput,
  type RetirementPlan,
  type RetirementState,
} from './retirement'
```

`packages/core/src/index.ts:54-86` 那段 `from './inject'` 具名导出里，在 `PACK_BACKUP_REL,` 之后插入五行：`RETIREMENT_STATES,`、`planRetirement,`、`type RetirementAction,`、`type RetirementFile,`、`type RetirementInput,`、`type RetirementPlan,`、`type RetirementState,`。

- [ ] **Step 5: 跑到通过**

Run: `npx vitest run --project unit packages/core/src/inject/`
Expected: PASS（RT-01..08 + 既有 planner/lock/security/sync-lock 全绿）

- [ ] **Step 6: 交棒**：变更清单 + 输出给 owner，等放行。

---

### Task 3: `cleanAction` 主干（CLI-CLEAN-01..06 = §7.10 a–f）

**Files:**
- Create: `apps/cli/src/commands/clean.ts`
- Create: `apps/cli/test/clean.test.ts`
- Modify: `apps/cli/src/commands/sync.ts:281`（`releaseLockOnSignal` 加 `export`）

**Interfaces:**
- Consumes: Task 2 的 `planRetirement`；`sync.ts` 已导出的 `utcStamp` / `uniqueBackupRoot` / `releaseLockOnSignal`；core 的 `PACK_LOCK_REL` / `parsePackLock` / `tryAcquireSyncLock`
- Produces: `cleanAction(options, deps)` → `CleanOutcome`；`CleanError`；`confirmWithClack`；`CleanReportRow`。Task 4/5 只在 outcome 上加字段，签名不变。

- [ ] **Step 1: 写失败测试**

创建 `apps/cli/test/clean.test.ts`（夹具与断言口径全部沿用 sync.test.ts：真盘 + 全树快照）：

```ts
import { existsSync, mkdirSync, mkdtempSync, readFileSync, rmSync, writeFileSync } from 'node:fs'
import { tmpdir } from 'node:os'
import { join } from 'node:path'
import { afterAll, describe, expect, it, vi } from 'vitest'
import { ADAPTER_MAIN_PATH } from '@openvibe/adapters' // T3 未用（落地时先删，否则 eslint no-unused-vars 红）；Task 4 的 07/08 起才真正引用
import {
  PACK_BACKUP_REL,
  PACK_LOCK_REL,
  parsePackLock,
  type RetirementState,
} from '@openvibe/core'
import { cleanAction, CleanError } from '../src/commands/clean'
// 05c/05d 要直接驱动 clack 的 module mock（覆盖 clean.ts 里的 confirmWithClack），故必须把这个命名导入引进来
import { confirm } from '@clack/prompts'
import { syncAction } from '../src/commands/sync'
import { demoPack, writeBundleFile, type PackFixture } from './helpers/pack-fixture'
import { putFile, treeSnapshot } from './helpers/tree'

/**
 * T10 · `openvibe clean`（m6b FR-6 + 验收 §7.10 a–j）。
 * 分支映射：01↔a 02↔b 03↔c 04↔d 05↔e 06↔f 07↔g 08↔h 09↔i 10↔j。
 * 判据一律落在磁盘实况与 lock 读回上——被保护的对象就是那些文件。
 */

vi.mock('@clack/prompts', () => ({
  select: vi.fn(async () => 'overwrite'),
  isCancel: vi.fn((v: unknown) => v === '__cancel__'),
  confirm: vi.fn(async () => true),
}))

const NOW = new Date('2026-09-24T10:20:30.400Z')
// 快照键恒为 `/` 分隔（helpers/tree.ts:19），而 *_REL 常量走 path.join ⇒ win32 上是反斜杠。
// 任何「拿快照键比 *_REL」的断言都必须先归一，否则该支在 CI 的 windows-latest 腿上必红（三平台绿是硬要求）。
// 单一模块级常量，别在各测试里各写一份局部 lockKey——那会让漏改第二处成为可能（T3 修复轮 1 就是这么翻车的）。
const PACK_LOCK_KEY = PACK_LOCK_REL.split(/[\\/]/).join('/')
const STAMP = '2026-09-24T10-20-30Z'
const roots: string[] = []

function sandbox(): { root: string; project: string } {
  const root = mkdtempSync(join(tmpdir(), 'ov-clean-test-'))
  roots.push(root)
  const project = join(root, 'proj')
  mkdirSync(project)
  return { root, project }
}

afterAll(() => {
  while (roots.length > 0) rmSync(roots.pop() ?? '', { recursive: true, force: true })
})

const fixture = (): PackFixture => demoPack({ targets: ['claude-code', 'cursor'] })

/** 已注入项目：返回 bundle 路径与包内容查表 */
async function injected(fixture: PackFixture) {
  const { root, project } = sandbox()
  const bundlePath = writeBundleFile(root, fixture)
  await syncAction({ projectPath: project, file: bundlePath, yes: true }, { now: () => NOW })
  return { root, project, bundlePath }
}

const stateMap = (rows: { path: string; state: RetirementState }[]): Record<string, string> =>
  Object.fromEntries(rows.map((r) => [r.path, r.state]))

describe('clean 默认退场（§7.10 a）', () => {
  it('CLI-CLEAN-01: 未改动的受管文件全删 + 逐字节进备份 + lock 删除 + .openvibe 仍在 + 退出码 0', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const paths = fx.files.map((f) => f.path)
    const expected = new Map(fx.files.map((f) => [f.path, f.content]))

    const outcome = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })

    for (const p of paths) expect(existsSync(join(project, p)), `${p} 应已删除`).toBe(false)
    for (const p of paths)
      expect(readFileSync(join(project, PACK_BACKUP_REL, STAMP, p), 'utf8'), `${p} 备份不一致`).toBe(
        expected.get(p),
      )
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(false)
    expect(existsSync(join(project, '.openvibe'))).toBe(true)
    expect(existsSync(join(project, PACK_BACKUP_REL))).toBe(true)
    expect(outcome.summary).toMatchObject({ inSyncRemoved: paths.length, driftKept: 0, cleaned: true })
    expect(outcome.exitCode).toBe(0)
  })

  it('CLI-CLEAN-01b: 退场后 diff 报未注入（NO_LOCK）', async () => {
    const { diffAction } = await import('../src/commands/diff')
    const { project } = await injected(fixture())
    await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })
    await expect(diffAction({ projectPath: project })).rejects.toMatchObject({ code: 'NO_LOCK' })
  })
})

describe('DRIFT 保留与 --force（§7.10 b、c）', () => {
  it('CLI-CLEAN-02: 手改 TERMS.md → 该文件原样在盘 + 退出码 2 + driftKept=1 + 其余照删', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    putFile(project, 'TERMS.md', '用户注入后手改的内容\n')

    const outcome = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })

    expect(readFileSync(join(project, 'TERMS.md'), 'utf8')).toBe('用户注入后手改的内容\n')
    expect(existsSync(join(project, 'CLAUDE.md'))).toBe(false)
    expect(outcome.summary.driftKept).toBe(1)
    expect(outcome.exitCode).toBe(2)
    expect(stateMap(outcome.report)['TERMS.md']).toBe('DRIFT')
    // lock 必须留着：它仍描述活着的受管文件，删了下次 sync 会把用户手改当陌生文件
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(true)
    const kept = parsePackLock(readFileSync(join(project, PACK_LOCK_REL), 'utf8'))
    expect(kept?.files.some((f) => f.path === 'TERMS.md')).toBe(true)
  })

  it('CLI-CLEAN-03: --force 删 DRIFT，且备份的是用户改后内容（不是包内版本）', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    putFile(project, 'TERMS.md', '用户注入后手改的内容\n')

    const outcome = await cleanAction({ projectPath: project, yes: true, force: true }, { now: () => NOW })

    expect(existsSync(join(project, 'TERMS.md'))).toBe(false)
    expect(readFileSync(join(project, PACK_BACKUP_REL, STAMP, 'TERMS.md'), 'utf8')).toBe(
      '用户注入后手改的内容\n',
    )
    expect(outcome.summary).toMatchObject({ driftForced: 1, driftKept: 0 })
    expect(outcome.exitCode).toBe(0)
  })
})

describe('零副作用与前置（§7.10 d、e、f）', () => {
  it('CLI-CLEAN-04: dry-run 全树快照逐字节不变，且不新建 .openvibe/', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const before = treeSnapshot(project)

    const outcome = await cleanAction({ projectPath: project, dryRun: true }, { now: () => NOW })

    expect(treeSnapshot(project)).toEqual(before)
    expect(outcome.dryRun).toBe(true)
    expect(outcome.summary.cleaned).toBe(false)
    expect(outcome.summary.inSyncRemoved).toBe(0)
    expect(outcome.report.every((r) => r.backupPath === undefined)).toBe(true)
  })

  it('CLI-CLEAN-04b: 无 lock 时 dry-run 也零副作用（不建 .openvibe/，不改一个字节）', async () => {
    const { project } = sandbox()
    putFile(project, 'README.md', '未注入过的项目\n')
    const before = treeSnapshot(project)
    await expect(cleanAction({ projectPath: project, dryRun: true })).rejects.toMatchObject({
      code: 'NO_LOCK',
    })
    expect(treeSnapshot(project)).toEqual(before)
  })

  it('CLI-CLEAN-04c: dry-run 不改 exitCode 语义（有 DRIFT 残留时仍为 2）', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    putFile(project, 'TERMS.md', '用户改过的术语表\n')
    const before = treeSnapshot(project)
    const dry = await cleanAction(
      { projectPath: project, dryRun: true },
      { isTTY: true, confirm: async () => false, now: () => NOW },
    )
    expect(dry.exitCode).toBe(2)
    expect(dry.summary.driftKept).toBe(1)
    expect(treeSnapshot(project)).toEqual(before)
    const real = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })
    expect(real.exitCode).toBe(dry.exitCode)
  })

  it('CLI-CLEAN-05: 非 TTY 且无 --yes → NEED_TTY，零删除', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const before = treeSnapshot(project)

    await expect(
      cleanAction({ projectPath: project }, { isTTY: false, now: () => NOW }),
    ).rejects.toMatchObject({ code: 'NEED_TTY' })
    expect(treeSnapshot(project)).toEqual(before)
    // 注意：首次全量 sync 不建 backup/（备份目录是惰性建的），所以这条是真断言而不是恒真式
    expect(existsSync(join(project, PACK_BACKUP_REL))).toBe(false)
  })

  it('CLI-CLEAN-05b: TTY 且用户在确认里选「否」→ 什么都不删、退出码 2、cleaned=false（§7.10 e2 / FR-6.9 v1.4）', async () => {
    const { project } = await injected(fixture())
    const before = treeSnapshot(project)
    const outcome = await cleanAction(
      { projectPath: project },
      { isTTY: true, confirm: async () => false, now: () => NOW },
    )
    expect(treeSnapshot(project)).toEqual(before)
    expect(outcome.summary.cleaned).toBe(false)
    expect(outcome.summary.inSyncRemoved).toBe(0)
    // 2 而非 0：0 的口径是「全清」，一次什么都没做的执行报 0 是假信号；1 的枚举里全是错误
    expect(outcome.exitCode).toBe(2)
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(true)
    // 取消必须留下可读痕迹：不能既什么都不做又不告诉用户为什么
    expect(outcome.hints.join('\n')).toContain('未删除')
  })

  it('CLI-CLEAN-05c: 真走 confirmWithClack（不注入 deps.confirm）——accept 即删、cancel 即退 2', async () => {
    const { project } = await injected(fixture())
    // 这一支刻意**不给 deps.confirm**，让 clean.ts 里的 confirmWithClack 与 clack 的 module mock 直接对话
    vi.mocked(confirm).mockResolvedValueOnce(false)
    const declined = await cleanAction({ projectPath: project }, { isTTY: true, now: () => NOW })
    expect(declined.exitCode).toBe(2)
    expect(declined.summary.cleaned).toBe(false)
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(true)
    expect(vi.mocked(confirm)).toHaveBeenCalledTimes(1)

    vi.mocked(confirm).mockResolvedValueOnce(true)
    const accepted = await cleanAction({ projectPath: project }, { isTTY: true, now: () => NOW })
    expect(accepted.summary.cleaned).toBe(true)
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(false)
    // 同一批文件第二次没有可删项，但 lock 已不存在 ⇒ NO_LOCK（退场收尾的幂等面）
    await expect(
      cleanAction({ projectPath: project }, { isTTY: true, now: () => NOW }),
    ).rejects.toMatchObject({ code: 'NO_LOCK' })
  })

  it('CLI-CLEAN-05d: clack 回传 cancel 哨兵（Ctrl-C）→ 与答「不」同口径，退出码 2 且零删除', async () => {
    const { project } = await injected(fixture())
    const before = treeSnapshot(project)
    vi.mocked(confirm).mockResolvedValueOnce('__cancel__' as unknown as boolean)
    const outcome = await cleanAction({ projectPath: project }, { isTTY: true, now: () => NOW })
    expect(outcome.exitCode).toBe(2)
    expect(outcome.summary.cleaned).toBe(false)
    expect(treeSnapshot(project)).toEqual(before)
  })

  it('CLI-CLEAN-06: 无 lock → NO_LOCK + 退出码语义 1 + 零删除', async () => {
    const { root, project } = sandbox()
    putFile(project, 'CLAUDE.md', '用户自己的文件\n')
    const before = treeSnapshot(project)
    const err = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW }).catch(
      (e: CleanError) => e,
    )
    expect((err as CleanError).code).toBe('NO_LOCK')
    expect((err as Error).message).toContain('未注入')
    expect(treeSnapshot(project)).toEqual(before)
    void root
  })

  it('CLI-CLEAN-06c: lock 存在但解析失败 → LOCK_INVALID + 一个字节都不删（§7.10 f2 / FR-6.2 安全侧）', async () => {
    const { project } = await injected(fixture())
    // 模拟用户/别的工具改坏了 lock：parsePackLock 对非 JSON 与 schema 不符都返回 null
    putFile(project, PACK_LOCK_REL, '{"schemaVersion":1,"pack":"不是对象"}\n')
    const before = treeSnapshot(project)

    const err = await cleanAction(
      { projectPath: project, yes: true },
      { now: () => NOW },
    ).catch((e: CleanError) => e)

    expect((err as CleanError).code).toBe('LOCK_INVALID')
    expect(treeSnapshot(project)).toEqual(before)
    expect(existsSync(join(project, PACK_BACKUP_REL))).toBe(false)
  })

  it('CLI-CLEAN-06b: 无可删项（全部 ABSENT：用户自己删过注入文件）→ 照样收尾并删 lock，cleaned=true（FR-6.5 v1.4 / §7.10 e3）', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    // 用户手工把注入产物删光：lock 还在，盘上一个都不在 ⇒ 全部 ABSENT，removals 为空
    const lock = parsePackLock(readFileSync(join(project, PACK_LOCK_REL), 'utf8'))
    expect(lock, 'sync 后应能读回 lock').not.toBeNull()
    for (const entry of lock?.files ?? []) rmSync(join(project, entry.path), { force: true })
    const before = treeSnapshot(project)

    const outcome = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })
    // 判据是 driftKept===0 而非「本次删了几个」：留着 lock 会让 diff 长报全量缺失且无命令可收尾
    expect(outcome.summary.absent).toBe(lock?.files.length ?? 0)
    expect(outcome.summary.inSyncRemoved).toBe(0)
    expect(outcome.summary.driftKept).toBe(0)
    expect(outcome.summary.cleaned).toBe(true)
    expect(outcome.exitCode).toBe(0)
    expect(existsSync(join(project, PACK_LOCK_REL))).toBe(false)
    expect(existsSync(join(project, PACK_BACKUP_REL))).toBe(false)
    // 除 lock 那一笔之外一个字节都没动。目录 mtime 会因 unlink 变化，故只对文件比内容
    const after = treeSnapshot(project)
    // 快照键恒为 `/` 分隔（helpers/tree.ts:19），而 PACK_LOCK_REL 走 join ⇒ win32 上是反斜杠。
    // 直接比 PACK_LOCK_REL 会让这两支在 CI 的 windows-latest 腿上红，故先归一（仓库惯例见 sync.test.ts:152）
    expect(Object.keys(before).filter((k) => !(k in after))).toEqual([PACK_LOCK_KEY])
    expect(Object.keys(after).filter((k) => !(k in before))).toEqual([])
    for (const [path, entry] of Object.entries(after)) {
      if (entry.kind !== 'file') continue
      expect(entry.sha256, `${path} 的内容不该被 clean 改动`).toBe(before[path]?.sha256)
    }
  })
})
```

- [ ] **Step 2: 跑到失败**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts`
Expected: FAIL —`Cannot find module '../src/commands/clean'`

- [ ] **Step 3: 实现 `cleanAction`**

> ✅ **T3 落地记录（2026-09-24，修复轮 `eb8e2f9`）——下方示例代码有三处已作废，按 `m6b v1.4` 重裁，别照抄**：
> **①** `sanitizeLockEntries` + `isPlainRelative`（本 Step 内联那份）已删除，替成 `clean.ts:126-143` 的 `rejectEscapingLockEntries`（只做逐条 `resolveWriteTarget`，专抓 schema 看不见的符号链接逃逸）；真正的「先净化后 schema」两道闸归 **Task 4 且在 core**（见 Task 4 头部的 ⚠️ 块）。
> **②** `nothingLeftManaged` 已删——「退场成功」判据是 **`driftKept === 0`**（FR-6.5 v1.4 ④），故 `cleaned = driftKept === 0`，全部 ABSENT / 全部 FOREIGN 都算收尾成功并删 lock。
> **③** `--dry-run` 与真跑共用同一条退出码导出（`base(rows, summary, exitCode = summary.driftKept > 0 ? 2 : 0)`），交互取消/`Ctrl-C` 显式走 **2**（FR-6.9 v1.4 ①）；测试多出 `04c/05c/05d/06d/06e/06f`，Task 3 实落 **17 支**。
> 另：快照键恒为 `/` 分隔，比 `PACK_LOCK_REL` 前必须 `split(/[\\/]/).join('/')` 归一，否则 win32 CI 腿必红。

创建 `apps/cli/src/commands/clean.ts`：

```ts
import { copyFileSync, existsSync, mkdirSync, readFileSync, statSync, unlinkSync } from 'node:fs'
import { dirname, join, resolve } from 'node:path'
import { confirm, isCancel } from '@clack/prompts'
import {
  PACK_LOCK_REL,
  SYNC_LOCK_STALE_MS,
  parsePackLock,
  planRetirement,
  resolveWriteTarget,
  tryAcquireSyncLock,
  type PackLock,
  type RetirementFile,
  type RetirementPlan,
  type RetirementState,
  type SyncLockAcquire,
  type SyncLockHandle,
} from '@openvibe/core'
import { compareCodeUnit } from '@openvibe/shared'
import { releaseLockOnSignal, uniqueBackupRoot, utcStamp } from './sync'

/**
 * `openvibe clean <projectPath>` —— 受管文件退场（m6b FR-6）。
 * sync 的反向半边：唯一输入是 pack.lock.json，而 lock 可被手改，所以这里的破坏面比 sync 更大。
 * 三道闸缺一不可——managed===true 才是删除凭据（FR-6.3）、删除前一律备份（FR-6.4）、
 * 路径净化失败即整包拒绝且合法条目也不删（FR-6.6，防「半退场」这种最难查的状态）。
 */

export class CleanError extends Error {
  readonly code: string
  readonly details?: unknown
  constructor(code: string, message: string, details?: unknown) {
    super(message)
    this.name = 'CleanError'
    this.code = code
    if (details !== undefined) this.details = details
  }
}

export interface CleanOptions {
  projectPath: string
  dryRun?: boolean
  yes?: boolean
  force?: boolean
}

export interface CleanDeps {
  isTTY?: boolean
  /** 一次批量确认（不是逐文件）：默认动作就是删，问一次足够 */
  confirm?: (plan: RetirementPlan) => Promise<boolean>
  now?: () => Date
}

export interface CleanReportRow {
  path: string
  state: RetirementState
  action: 'delete' | 'keep' | 'none'
  backupPath?: string
}

export interface CleanSummary {
  inSyncRemoved: number
  driftKept: number
  driftForced: number
  absent: number
  foreign: number
  backedUpTo: string | null
  cleaned: boolean
}

export interface CleanOutcome {
  projectPath: string
  lockPath: string
  pack: PackLock['pack']
  dryRun: boolean
  report: CleanReportRow[]
  summary: CleanSummary
  warnings: string[]
  hints: string[]
  exitCode: 0 | 2
}

function assertProjectDir(p: string): string {
  const abs = resolve(p)
  if (!existsSync(abs) || !statSync(abs).isDirectory()) {
    throw new CleanError('NOT_A_DIRECTORY', `项目路径不是目录：${abs}`)
  }
  return abs
}

/** 读取失败必须抛：把它当成「文件不存在」= 当成 ABSENT，就会漏掉 lock 里的一次真实存在 */
function diskReader(projectPath: string): (relPath: string) => string | null {
  return (relPath) => {
    const abs = resolveWriteTarget(projectPath, relPath)
    if (abs === null) {
      throw new CleanError('PATH_ESCAPE', `${relPath} 解析后不在项目内，拒绝作为基准`, {
        escapingPaths: [relPath],
      })
    }
    try {
      return readFileSync(abs, 'utf8')
    } catch (e) {
      const err = e as NodeJS.ErrnoException
      if (err.code === 'ENOENT' || err.code === 'ENOTDIR') return null
      throw new CleanError('READ_FAILED', `读取 ${abs} 失败：${err.message}`)
    }
  }
}

/** FR-6.6：净化失败整包拒绝，且聚合形状与 sync 的 VALIDATION_ERROR 同构（index.ts 的 violationLines 直接复用） */
function sanitizeLockEntries(projectPath: string, lock: PackLock): void {
  const invalidPaths: string[] = []
  const escapingPaths: string[] = []
  for (const entry of lock.files) {
    const abs = resolveWriteTarget(projectPath, entry.path)
    if (abs === null) escapingPaths.push(entry.path)
    else if (!existsSync(abs) && !isPlainRelative(entry.path)) invalidPaths.push(entry.path)
  }
  if (invalidPaths.length > 0 || escapingPaths.length > 0) {
    throw new CleanError(
      'VALIDATION_ERROR',
      `pack.lock.json 有 ${invalidPaths.length + escapingPaths.length} 条路径未通过净化，整包拒绝退场（其余条目同样不删）`,
      { invalidPaths: invalidPaths.sort(compareCodeUnit), escapingPaths: escapingPaths.sort(compareCodeUnit) },
    )
  }
}

function isPlainRelative(relPath: string): boolean {
  return !relPath.startsWith('/') && !relPath.startsWith('\\') && !relPath.includes('..')
}

type LockBusy = Extract<SyncLockAcquire, { acquired: false }>

/** 让路文案承 §6.9：说清谁在写、写了多久、下一步做什么 */
function busyMessage(path: string, lock: LockBusy): string {
  const seconds = Math.round(lock.ageMs / 1000)
  if (lock.reason === 'unavailable') {
    return `无法建立退场锁 ${path}（${lock.detail ?? '未知错误'}）：.openvibe 不可写或不是目录，先修目录再退场`
  }
  const who = lock.holder
    ? `pid ${String(lock.holder.pid)}（${lock.holder.command}，起于 ${lock.holder.startedAt}）`
    : '一把内容读不懂的残留锁'
  return `另一个写盘命令正在占用这个项目：${who}，已 ${String(seconds)}s。等它结束后重跑 clean 即可`
}

export const confirmWithClack = async (plan: RetirementPlan): Promise<boolean> => {
  const answer = await confirm({
    message: `将删除 ${String(plan.counts.IN_SYNC)} 个受管文件（先备份到 .openvibe/backup/），保留 ${String(plan.counts.DRIFT)} 个已改动的文件`,
    initialValue: false,
  })
  return !isCancel(answer) && answer === true
}

export async function cleanAction(
  options: CleanOptions,
  deps: CleanDeps = {},
): Promise<CleanOutcome> {
  const projectPath = assertProjectDir(options.projectPath)
  const lockPath = join(projectPath, PACK_LOCK_REL)
  let text: string
  try {
    text = readFileSync(lockPath, 'utf8')
  } catch {
    throw new CleanError(
      'NO_LOCK',
      `${lockPath} 不存在：该项目未注入过标准包，无需退场。`,
    )
  }
  const lock = parsePackLock(text)
  if (!lock) {
    throw new CleanError(
      'LOCK_INVALID',
      `${lockPath} 无法解析或含非法文件路径：被改过的 lock 不能当删除依据。` +
        '删掉它即承认这些文件归你所有（本命令不会替你删）',
    )
  }
  sanitizeLockEntries(projectPath, lock)

  const plan = planRetirement({
    lock,
    readDisk: diskReader(projectPath),
    ...(options.force === true ? { force: true } : {}),
  })
  const dryRun = options.dryRun === true
  const warnings: string[] = []
  const hints: string[] = []

  const base = (
    rows: CleanReportRow[],
    summary: CleanSummary,
  ): CleanOutcome => ({
    projectPath,
    lockPath,
    pack: lock.pack,
    dryRun,
    report: rows,
    summary,
    warnings,
    hints,
    exitCode: summary.driftKept > 0 ? 2 : 0,
  })

  const rowsOf = (files: RetirementFile[], backup: Map<string, string>): CleanReportRow[] =>
    files.map((f) => ({
      path: f.path,
      state: f.state,
      action: f.action,
      ...(backup.has(f.path) ? { backupPath: backup.get(f.path) } : {}),
    }))

  const tally = (removed: number, forced: number, kept: number, backedUpTo: string | null, cleaned: boolean): CleanSummary => ({
    inSyncRemoved: removed,
    driftKept: kept,
    driftForced: forced,
    absent: plan.counts.ABSENT,
    foreign: plan.counts.FOREIGN,
    backedUpTo,
    cleaned,
  })

  if (dryRun) {
    hints.push('dry-run：以上为计划，零写入零删除（不建 .openvibe/、不抢锁）')
    if (plan.removals.length > 0)
      warnings.push(`dry-run：${String(plan.removals.length)} 项将被删除，正式执行需 --yes 或交互确认`)
    return base(rowsOf(plan.files, new Map()), tally(0, 0, plan.counts.DRIFT, null, false))
  }

  if (plan.removals.length > 0 && options.yes !== true) {
    if (deps.isTTY !== true) {
      throw new CleanError(
        'NEED_TTY',
        `${String(plan.removals.length)} 个文件需要删除确认，但当前不是交互终端：加 --yes（仅确认默认动作）` +
          `；要连已改动的 ${String(plan.counts.DRIFT)} 个一起删还得加 --force`,
        { removals: [...plan.removals].sort(compareCodeUnit) },
      )
    }
    const granted = await (deps.confirm ?? confirmWithClack)(plan)
    if (!granted) {
      hints.push('已取消：未删除任何文件，lock 原样保留')
      return base(rowsOf(plan.files, new Map()), tally(0, 0, plan.counts.DRIFT, null, false))
    }
  }

  let handle: SyncLockHandle | null = null
  if (plan.removals.length > 0) {
    const acquired = tryAcquireSyncLock(projectPath, 'clean')
    if (!acquired.acquired) throw new CleanError('SYNC_BUSY', busyMessage(acquired.path, acquired))
    handle = acquired
    if (acquired.tookOver !== null) {
      const prev = acquired.tookOver
      hints.push(
        `接管了残留写盘锁（pid ${String(prev.pid)}，${prev.command}，起于 ${prev.startedAt}）` +
          `：该进程已不存在或超过 ${String(Math.round(SYNC_LOCK_STALE_MS / 1000))}s 未更新`,
      )
    }
  }
  const stopSignalHandlers = handle ? releaseLockOnSignal(handle) : null

  try {
    const now = (deps.now ?? (() => new Date()))()
    const backup = new Map<string, string>()
    let backupRoot: string | null = null
    const removedPaths = new Set<string>()

    for (const relPath of plan.removals) {
      const abs = resolveWriteTarget(projectPath, relPath)
      if (abs === null || !existsSync(abs)) continue // 规划后被别人删了：按 ABSENT 处理
      if (backupRoot === null) {
        backupRoot = uniqueBackupRoot(projectPath, utcStamp(now))
        mkdirSync(backupRoot, { recursive: true })
      }
      const backupPath = join(backupRoot, relPath)
      try {
        mkdirSync(dirname(backupPath), { recursive: true })
        copyFileSync(abs, backupPath)
        unlinkSync(abs)
      } catch (e) {
        throw new CleanError(
          'DELETE_FAILED',
          `退场 ${abs} 失败：${(e as Error).message}。已完成 ${String(removedPaths.size)}/${String(plan.removals.length)} 项` +
            (backupRoot ? `；已删文件在 ${backupRoot} 有逐字节副本，可逐个复制回去` : ''),
          { done: [...removedPaths] },
        )
      }
      backup.set(relPath, backupPath)
      removedPaths.add(relPath)
    }

    // lock 只在「注入已不残留在盘上」时删除：还留着 DRIFT 就仍描述活文件，删了下次 sync 会误判
    const driftKept = plan.files.filter((f) => f.state === 'DRIFT' && f.action === 'keep').length
    const nothingLeftManaged = removedPaths.size > 0 || plan.counts.ABSENT === lock.files.length
    let cleaned = driftKept === 0 && nothingLeftManaged
    if (cleaned) {
      try {
        unlinkSync(lockPath)
      } catch (e) {
        cleaned = false
        warnings.push(`pack.lock.json 删除失败（${(e as Error).message}）：受管文件已退场，请手工删除该登记文件`)
      }
    }

    hints.push('确认无误后可手工删除 .openvibe/（本命令不动它，备份目录是你唯一的回退凭据）')
    hints.push('未改动 .gitignore：建议自行把 .openvibe/backup/ 加入忽略清单')

    const driftForced = plan.files.filter((f) => f.state === 'DRIFT' && f.action === 'delete').length
    return base(
      rowsOf(plan.files, backup),
      tally(
        plan.files.filter((f) => f.state === 'IN_SYNC' && removedPaths.has(f.path)).length,
        driftForced,
        driftKept,
        backupRoot,
        cleaned,
      ),
    )
  } finally {
    if (stopSignalHandlers) stopSignalHandlers()
    if (handle) handle.release()
  }
}
```

> 两处易被「顺手优化」掉的点：① `cleaned` 必须声明成 `let`——`catch` 分支要把它改回 `false`，linter 报 prefer-const 说明那条降级分支被删了，那是回归而不是风格问题；② 备份目录的创建**推迟到第一个真要删的文件**（`backupRoot === null` 才 `mkdirSync`），否则「无可删项」的一次 clean 会在项目里凭空建出 `.openvibe/backup/`，与 §7.10 f 的「零删除」自相矛盾。

- [ ] **Step 4: 导出信号处理器**

`apps/cli/src/commands/sync.ts:281` 把 `function releaseLockOnSignal(` 改为 `export function releaseLockOnSignal(`。这是刻意的复用：放锁 + 摘信号监听这一对动作有个已验证的时序约束（sync.test.ts / sync-lock.test.ts 都依赖它），复制一份到 clean.ts 等于制造第二份会漂移的实现。

- [ ] **Step 5: 跑到通过**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts`
Expected: PASS（本轮实际落 `01,01b,02,03,04,04b,04c,05,05b,05c,05d,06,06b,06c,06d,06e,06f` = **17 支**；`06d/06e/06f` 是 T3 修复轮为 I-1/I-3/M-2 补的实盘支，`07..12` 属后续任务）

Run: `npx vitest run --project cli apps/cli/test/sync.test.ts`
Expected: PASS（导出一个符号不应改变 sync 行为）

- [ ] **Step 6: 交棒**：变更清单 + 两支测试输出，等放行。

---

### Task 4: 净化整包拒绝与并发锁（CLI-CLEAN-07..08 = §7.10 g、h）

> ⚠️ **本任务的净化半边已按 `m6b v1.4` ②③ 重裁。下方所有「在 `clean.ts` 里写/收紧 `sanitizeLockEntries`、`isPlainRelative`、`details.invalidPaths`」的示例代码，以及 Step 2 里那条「预期失败原因」，一并作废**——T3 修复轮已经把那段 CLI 侧死复制净化删掉，现存的是 `apps/cli/src/commands/clean.ts:126-143` 的 `rejectEscapingLockEntries`（只做逐条 `resolveWriteTarget`，专抓 schema 看不见的符号链接逃逸，是活代码）。照抄下方代码等于把 T3 刚删掉的死码再抄一遍。
> 规格口径（`docs/specs/m6-cli-injection.md` FR-6.6 v1.4）：
> **① 净化归 core**——不在 CLI 里再造第二份路径规则（薄客户端；与 `checkPackInjectable` 同侧，落点建议 `packages/core/src/inject/security.ts` 的聚合式判定：一次跑完全部规则、按码点序出清单）。
> **② 路径净化必须早于 `PackLockSchema` 整机校验**——`packPathSchema`（`packages/shared/src/schemas/pack.ts:130` = `isValidPackRelativePath`）会先把含 `..` 的 lock 整份判成 schema 不符；顺序反过来就只剩 §7.10 f2 的 `LOCK_INVALID`，而 §7.10 g 要求的「报告含违规路径」永远出不来（两道闸互吃：g 那把被篡改的 lock 恰好就是 schema 不符的 lock）。
> **③ 落地形状**：宽松读出 JSON → 取 `files[].path` 字符串逐条净化（core 的 `isValidPackRelativePath` 管 `..`/绝对路径/超长，`resolveWriteTarget` 管符号链接逃逸）→ 任一违规**整包拒绝**、退出码 1、`details` 点名违规项、**同 lock 内的合法条目也不删**；JSON 读不出、或路径都合法而其它字段不符 ⇒ 才落 `LOCK_INVALID`。§7.10 f / f2 / g 三支同时绿，考的就是这个先后顺序。
> **并发锁那半边（§7.10 h / `CLI-CLEAN-08`）不受重裁影响，照原计划做。**

**Files:**
- Modify: `apps/cli/test/clean.test.ts`（追加两个 describe：`CLI-CLEAN-07` 净化、`08` 并发锁）
- Modify: `packages/core/src/inject/security.ts`（**净化闸的正主**：聚合式、先于 schema）
- Modify: `apps/cli/src/commands/clean.ts`（把闸前移到 `parsePackLock` 之前并改调 core；**不要**在 CLI 里重写路径规则）

**Interfaces:**
- Consumes: Task 3 的 `cleanAction`、`apps/cli/test/helpers/sync-lock-holder.ts`（command 写 `sync-holder`，clean 只需「有人持锁」这一事实，无需新夹具）
- Produces: 净化失败时的 `details` 形状 `{invalidPaths?, escapingPaths}`（Task 5 的 `violationLines` 渲染依赖它，且 `apps/cli/src/index.ts` 要能容忍 `invalidPaths` 缺位）

- [ ] **Step 1: 写失败测试**

在 `clean.test.ts` 追加（顶部 import 补 `spawnSync`, `fileURLToPath`, `dirname`, `SYNC_LOCK_REL`, `PackLock` 类型）：

```ts
describe('lock 净化与并发（§7.10 g、h）', () => {
  it('CLI-CLEAN-07: 篡改 lock 使某条 path 为 ../evil.txt → 整包拒绝，合法条目也不删', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const lockPath = join(project, PACK_LOCK_REL)
    const lock = JSON.parse(readFileSync(lockPath, 'utf8'))
    lock.files[0].path = '../evil.txt'
    writeFileSync(lockPath, `${JSON.stringify(lock, null, 2)}\n`, 'utf8')
    const before = treeSnapshot(project)
    const outside = join(dirname(project), 'evil.txt')

    const err = await cleanAction({ projectPath: project, yes: true, force: true }, { now: () => NOW }).catch(
      (e: CleanError) => e,
    )

    expect((err as CleanError).code).toBe('VALIDATION_ERROR')
    expect((err as Error).message).toContain('整包拒绝')
    expect((err as CleanError).details).toMatchObject({ escapingPaths: ['../evil.txt'] })
    expect(existsSync(outside)).toBe(false)
    expect(treeSnapshot(project)).toEqual(before)
  })

  it('CLI-CLEAN-08: 另一进程持 sync.lock 时 clean --yes → SYNC_BUSY、零删除、他人的锁原样在', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const holder = spawnSync(process.execPath, ['--import', 'tsx', HOLDER, project, '4000'], {
      encoding: 'utf8',
      stdio: ['pipe', 'pipe', 'pipe'],
    })
    // 夹具用 stdin 生命周期持锁，spawnSync 拿不到异步握手 → 改后台起 + 轮询锁文件出现
    void holder
    const child = spawn(process.execPath, ['--import', 'tsx', HOLDER, project], { stdio: ['pipe', 'pipe', 'pipe'] })
    const lockAbs = join(project, SYNC_LOCK_REL)
    for (let i = 0; i < 100 && !existsSync(lockAbs); i += 1) await new Promise((r) => setTimeout(r, 50))
    expect(existsSync(lockAbs), '夹具未在 5s 内抢到锁').toBe(true)
    const lockBefore = readFileSync(lockAbs, 'utf8')
    const before = treeSnapshot(project)

    const outcome = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW }).catch(
      (e: CleanError) => e,
    )

    expect((outcome as CleanError).code).toBe('SYNC_BUSY')
    expect(treeSnapshot(project)).toEqual(before)
    expect(readFileSync(lockAbs, 'utf8')).toBe(lockBefore)
    child.stdin.end()
    await new Promise((r) => setTimeout(r, 300))
  })
})
```

并在文件顶部常量区补：

```ts
const HERE = dirname(fileURLToPath(import.meta.url))
const HOLDER = join(HERE, 'helpers', 'sync-lock-holder.ts')
```

> 上面 `spawnSync(...)` 那一行是故意留在计划里的**反面样本**：夹具靠 stdin 持锁，`spawnSync` 会在读满 stdout 后一直等，测试随之挂住。写测试时**删掉那三行**（连同 `void holder`），只保留 `spawn` 那条路径——保留它的价值是让下一位读者不必重新踩。

- [ ] **Step 2: 跑到失败**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts -t 'CLI-CLEAN-07'`
Expected: FAIL —— **失败在错误码上，而不是断言逻辑上**：现行 `cleanAction` 先跑 `parsePackLock`（`:181` 附近），而 `packPathSchema` 会把含 `../evil.txt` 的 lock **整份**判成 schema 不符 ⇒ 抛的是 `LOCK_INVALID`、`details` 里没有违规路径清单。这正是 `m6b v1.4` ② 说的两道闸互吃，也是本任务要修的唯一根因：**净化必须早于整机 schema 校验**。
（`CLI-CLEAN-08` 的并发支应当一次就绿——T3 已把 `tryAcquireSyncLock` 的复用面留好了；若它也红，那是回归，先修回归。）

- [ ] **Step 3: 把净化闸做成 core 的前置闸**

**不要**在 `clean.ts` 里写路径规则（薄客户端；T3 修复轮刚删掉一份弱复制）。落在 `packages/core/src/inject/security.ts`，与 `checkPackInjectable` 同侧、同风格（一次跑完全部规则、按码点序出清单）：

```ts
/**
 * clean 的 lock 路径净化（m6b FR-6.6 v1.4）——**必须早于 `PackLockSchema` 整机校验**。
 * 理由已写进规格：`packPathSchema`（shared/schemas/pack.ts:130 = isValidPackRelativePath）
 * 会先把含 `..` 的 lock 整份判成 schema 不符，顺序反了就只剩 LOCK_INVALID，
 * §7.10 g 要的「报告含违规路径」永远出不来（g 与 f2 互吃）。
 *
 * 入参是**宽松读出的 JSON**（`unknown`），不是 `PackLock`：能读出 `files[].path` 字符串就报得出违规项。
 * 违规项一律记进 `escapingPaths` 单桶——对 clean 而言「malformed」与「escaping」是同一种危险
 * （resolve 后落在项目外），`invalidPaths` 那桶留给 sync 的 bundle 侧，别在这里造第二套词。
 */
export function auditLockPaths(projectPath: string, raw: unknown): { escapingPaths: string[] }
```

`apps/cli/src/commands/clean.ts` 侧只做三件事，顺序即口径：**读文件 → `JSON.parse`（失败就直接落 `LOCK_INVALID`，此时无人能报出违规项）→ `auditLockPaths` 有违规就整包拒绝 → 才 `parsePackLock`**。

错误码保持两枚、不要合并：前置聚合闸抛 `VALIDATION_ERROR`（`details.escapingPaths` 点名违规项，§7.10 g），删除循环里的 `resolveWriteTarget === null` 仍抛 `PATH_ESCAPE`（TOCTOU 兜底，T3 修复轮 I-3）。两枚都归 FR-6.9 的退出码 `1`，`apps/cli/src/index.ts` 的 `violationLines` 已能容忍 `invalidPaths` 缺位。

- [ ] **Step 4: 跑到通过**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts`
Expected: PASS（含 01..08）

- [ ] **Step 5: 交棒**：两支新用例的输出 + core 侧 `auditLockPaths` 与 CLI 侧调用顺序的 diff（含「§7.10 f/f2/g 三支同时绿」的证据），等放行。

---

### Task 5: 可重放律、`managed:false` 闸与命令注册（CLI-CLEAN-09..10 = §7.10 i、j）

**Files:**
- Modify: `apps/cli/test/clean.test.ts`（追加末两支 + 一支 `--json` 契约用例）
- Modify: `apps/cli/src/index.ts`（`import` + `runClean` + `program.command('clean')`）

**Interfaces:**
- Consumes: Task 3 的 `cleanAction` / `CleanOutcome`；`output.ts` 的 `createPrinter` / `ExitCode` / `ReportRow`
- Produces: `openvibe clean` 命令行（`--dry-run` / `--yes` / `--force` / `--json`）与 FR-6.10 的 `--json` 形状

- [ ] **Step 1: 写失败测试（可重放律与删除凭据闸）**

```ts
describe('可重放律与 managed 闸（§7.10 i、j）', () => {
  it('CLI-CLEAN-09: sync → clean --yes → 再 sync → diff 退出码 0（退场不残留污染态）', async () => {
    const fx = fixture()
    const { project, bundlePath } = await injected(fx)
    const first = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })
    expect(first.summary.cleaned).toBe(true)

    await syncAction({ projectPath: project, file: bundlePath, yes: true }, { now: () => NOW })
    const { diffAction } = await import('../src/commands/diff')
    const again = await diffAction({ projectPath: project })
    expect(again.exitCode).toBe(0)
    expect(again.clean).toBe(true)
  })

  it('CLI-CLEAN-10: managed:false 不构成删除凭据——用户自有的同名同字节文件留在原样且不进备份', async () => {
    const fx = fixture()
    const { root, project } = sandbox()
    const bundlePath = writeBundleFile(root, fx)
    const claude = fx.files.find((f) => f.path === 'CLAUDE.md')
    expect(claude, '夹具缺 CLAUDE.md 产物').toBeTruthy()
    // 用户自己写了个同名文件，内容与包逐字节相同（Task 1 §7.10j 的构造）
    putFile(project, 'CLAUDE.md', claude?.content ?? '')
    await syncAction(
      { projectPath: project, file: bundlePath, yes: true, targets: ['cursor'] },
      { now: () => NOW },
    )
    const lock = parsePackLock(readFileSync(join(project, PACK_LOCK_REL), 'utf8'))
    expect(lock?.files.find((f) => f.path === 'CLAUDE.md')?.managed).toBe(false)

    const outcome = await cleanAction({ projectPath: project, yes: true }, { now: () => NOW })

    expect(readFileSync(join(project, 'CLAUDE.md'), 'utf8')).toBe(claude?.content)
    expect(stateMap(outcome.report)['CLAUDE.md']).toBe('FOREIGN')
    expect(existsSync(join(project, ADAPTER_MAIN_PATH.cursor))).toBe(false)
    expect(existsSync(join(project, PACK_BACKUP_REL, STAMP, ADAPTER_MAIN_PATH.cursor))).toBe(true)
    expect(existsSync(join(project, PACK_BACKUP_REL, STAMP, 'CLAUDE.md'))).toBe(false)
    expect(outcome.exitCode).toBe(0)
  })
})
```

- [ ] **Step 2: 写 `--json` 契约用例（真子进程，形状即 FR-6.10）**

同文件追加（import 补 `spawnSync`, `CLI_ENTRY`）：

```ts
const CLI_ENTRY = join(dirname(fileURLToPath(import.meta.url)), '..', 'src', 'index.ts')

describe('命令注册与 --json 契约（FR-6.10）', () => {
  const runClean = (args: string[], home: string) =>
    spawnSync(process.execPath, ['--import', 'tsx', CLI_ENTRY, '--json', 'clean', ...args], {
      encoding: 'utf8',
      timeout: 25_000,
      env: { ...process.env, OPENVIBE_HOME: home, OPENVIBE_TOKEN: '', OPENVIBE_SERVER: '' },
    })

  it('CLI-CLEAN-11: `clean --help` 在命令总览里；--json 输出形状 = {command,report,summary{6+键}}；退出码 0', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    const home = mkdtempSync(join(tmpdir(), 'ov-clean-home-'))
    roots.push(home)

    const out = runClean([project, '--yes'], home)
    expect(out.status, out.stderr).toBe(0)
    const env = JSON.parse(out.stdout) as {
      command: string
      report: { path: string; state: string; action: string }[]
      summary: Record<string, unknown>
    }
    expect(env.command).toBe('clean')
    expect(env.report.length).toBe(fx.files.length)
    expect(env.summary).toMatchObject({ backedUpTo: expect.any(String), cleaned: true })
    for (const key of ['inSyncRemoved', 'driftKept', 'driftForced', 'absent', 'backedUpTo', 'cleaned'])
      expect(Object.hasOwn(env.summary, key), `summary 缺键 ${key}`).toBe(true)
    expect(env.report.every((r) => ['IN_SYNC', 'DRIFT', 'ABSENT', 'FOREIGN'].includes(r.state))).toBe(true)
  })

  it('CLI-CLEAN-11b: 有 DRIFT 残留时 --json ⇒ 退出码 2 且 summary.ok === false（`ok` 与退出码不得各说各话）', async () => {
    const fx = fixture()
    const { project } = await injected(fx)
    // 造一条真 DRIFT：注入后手改受管的 TERMS.md（构造与进程内的 CLI-CLEAN-02:167 同一条，区别只在走真子进程）
    putFile(project, 'TERMS.md', '用户注入后手改的内容\n')
    const home = mkdtempSync(join(tmpdir(), 'ov-clean-home-'))
    roots.push(home)

    const out = runClean([project, '--yes'], home)

    expect(out.status).toBe(2)
    const env = JSON.parse(out.stdout) as { summary: { ok: boolean; exitCode: number; driftKept: number } }
    expect(env.summary.ok).toBe(false)
    expect(env.summary.exitCode).toBe(2)
    expect(env.summary.driftKept).toBe(1)
  })

  it('CLI-CLEAN-12: 无 lock 时 --json 仍是一个对象且 summary.ok=false + code=NO_LOCK，退出码 1', () => {
    const { project } = sandbox()
    const home = mkdtempSync(join(tmpdir(), 'ov-clean-home-'))
    roots.push(home)
    const out = runClean([project, '--yes'], home)
    expect(out.status).toBe(1)
    const env = JSON.parse(out.stdout) as { command: string; summary: { ok: boolean; error: { code: string } } }
    expect(env.command).toBe('clean')
    expect(env.summary.ok).toBe(false)
    expect(env.summary.error.code).toBe('NO_LOCK')
  })
})
```

> `CLI-CLEAN-11/11b/12` 是超出 §7.10 a–j 的**契约支**（命令注册与 `--json` 形状必须有人守），登记时如实写成「a–j 十支 + 契约三支」，**不要抄任何历史总数**（本行在 T4 之前写的是「12 支」，T4 之后实况已变——总数一律按 Task 9 的现测口径给）。

- [ ] **Step 3: 跑到失败**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts`
Expected: FAIL —`unknown command 'clean'`（CLI-CLEAN-11/12）；CLI-CLEAN-09/10 此时应已 PASS（Task 3/4 的实现即满足），若 09/10 也红，按报告修 core 分类器而不是改断言。

- [ ] **Step 4: 注册命令**

`apps/cli/src/index.ts` 顶部 import 段（`diffAction` 之后）：

```ts
import { cleanAction, type CleanOutcome } from './commands/clean'
```

在 `runDiff` 之后插入：

```ts
// 逐字段列出，**不能** `return [...outcome.report]`：`CleanReportRow` 是 interface（无隐式索引签名），
// 直传给 `ReportRow = Record<string, unknown>`（`output.ts:26`）过不了 tsc（TS2322）。
// 同文件 `diffRows`/`reportRowsOf` 就是这个写法，别在 Task 3 的导出面上动刀。
function cleanRows(outcome: CleanOutcome): ReportRow[] {
  return outcome.report
    .map(({ path, state, action, backupPath }) => ({ path, state, action, backupPath }))
    .sort((a, b) => compareCodeUnit(String(a.path), String(b.path)))
}

interface CleanCmdOptions {
  dryRun?: boolean
  yes?: boolean
  force?: boolean
}

async function runClean(projectPath: string, cmd: CleanCmdOptions, global: GlobalOptions): Promise<void> {
  const printer = printerFor('clean', global)
  const isTTY = process.stdout.isTTY === true
  try {
    const outcome = await cleanAction(
      {
        projectPath,
        dryRun: cmd.dryRun === true,
        yes: cmd.yes === true,
        force: cmd.force === true,
      },
      { isTTY },
    )
    for (const w of outcome.warnings) printer.warn(w)
    for (const row of outcome.report)
      printer.info(
        `${row.state.padEnd(8)} ${row.path}${row.backupPath ? `  备份 ${row.backupPath}` : ''}`,
      )
    const s = outcome.summary
    printer.info(
      `删除 ${String(s.inSyncRemoved + s.driftForced)}（受管未改动 ${String(s.inSyncRemoved)} / --force ${String(s.driftForced)}），` +
        `保留 ${String(s.driftKept)}，已自行退场 ${String(s.absent)}，非我方文件 ${String(s.foreign)}（永不删除）`,
    )
    if (s.backedUpTo) printer.info(`备份 ${s.backedUpTo}`)
    printer.info(s.cleaned ? '已删除 pack.lock.json；.openvibe/ 与备份目录保留' : 'pack.lock.json 未动')
    for (const hint of outcome.hints) printer.info(`提示 ${hint}`)
    // `ok` 必须**从退出码推导**，与 `runDiff`（`:337` `ok: outcome.exitCode === ExitCode.ok`）同式。
    // 写死 `ok: true` 的话，「有 DRIFT 残留 ⇒ 退出码 2」那一轮会在 JSON 里同时报 `ok: true` 与 `exitCode: 2`，
    // 机器读者只能挑一个信——而 `--json` 的全部意义就是不必挑。契约支 CLI-CLEAN-11b 钉这条。
    // 键名不叫 counts：printer 一见 counts 就渲染注入侧的 SYNC_STATES 五态表，clean 是四态。
    // hints 必带（m6b FR-6.10 v1.5）：交互取消那一腿在枚举键里没有任何对应，理由只活在 hints 里，
    // 而人读轨的 printer.info 在 JSON 轨是 no-op。顺序也要对——放在 ...s 之后，别让展开把它盖掉。
    printer.result({
      report: cleanRows(outcome),
      summary: {
        ok: outcome.exitCode === ExitCode.ok,
        ...s,
        hints: outcome.hints,
        exitCode: outcome.exitCode,
      },
    })
    process.exit(outcome.exitCode)
  } catch (e) {
    const err = e as { code?: string; details?: unknown; message?: string }
    for (const line of violationLines(err.details)) printer.warn(line)
    printer.fail(err.code ?? 'CLEAN_FAILED', err.message ?? String(e))
    process.exit(ExitCode.error)
  }
}
```

`buildProgram()` 内、`diff` 命令块之后：

```ts
program
  .command('clean')
  .description('退场：删除当前 lock 登记且未被改动的受管文件（删除前一律备份）')
  .argument('<projectPath>', '目标项目目录')
  .option('--dry-run', '只打印三态计划表，零写入零删除')
  .option('--yes', '确认默认动作（不升级为全删：要连改过的文件一起删得给 --force）')
  .option('--force', '连 DRIFT（用户注入后改过）的文件一并删除，改后内容先进备份目录')
  .action(async (projectPath: string, opts: CleanCmdOptions) => {
    await runClean(projectPath, opts, program.opts<GlobalOptions>())
  })
```

顶部 `@openvibe/shared` 的 import 里补 `compareCodeUnit`（现已导入 `compareCodeUnit` 于 index.ts:11，直接用）。

- [ ] **Step 5: 跑到通过 + 全闸**

Run: `npx vitest run --project cli apps/cli/test/clean.test.ts` → Expected: PASS（新增的 `09`/`10`/`11`/`11b`/`12` 与既有各支全绿；**总数按实跑输出登记，不在此写死**）
Run: `npx vitest run --project cli` → Expected: PASS（sync/diff/e2e 无回归）
Run: `pnpm lint && pnpm typecheck` → Expected: 0 问题

- [ ] **Step 6: 交棒**：clean 全套输出 + `index.ts` diff，等放行。

---

### Task 6: `estimateTokens` 纯函数（CORE-SIZE-01..05 = m6a 验收 8）

**Files:**
- Create: `packages/core/src/pack/size.ts`
- Create: `packages/core/src/pack/size.test.ts`
- Modify: `packages/core/src/pack/index.ts`
- Modify: `packages/core/src/index.ts:37-53`（`from './pack'` 那段具名导出）

**Interfaces:**
- Consumes: 无（纯函数，不依赖 DB / HTTP / adapters）
- Produces: `estimateTokens(content): TokenEstimate`、`SIZE_WARN_THRESHOLD = 12000`、`estimateBundle(contentList): number`

- [ ] **Step 1: 写失败测试**

创建 `packages/core/src/pack/size.test.ts`（数字已实量：空 0 / 401 ASCII 101 / 20 汉字 24 / `a😀b` 码点 3 而 `String.length` 4 / `汉字abc` 4 / `提示词，包含全角标点。` 11 码点全 CJK → 14）：

```ts
import { describe, expect, it } from 'vitest'
import { estimateTokens, SIZE_WARN_THRESHOLD } from './size'

/**
 * m6a FR-6.2 + 验收 8。这些数字是**规格里的口径**（粗估常数，不是任何 tokenizer 实测曲线），
 * 改动系数即 B 级变更：数字会进 TERMS.md 相关的展示与 warn 判定。
 */
describe('estimateTokens（CORE-SIZE-01..05）', () => {
  it('CORE-SIZE-01: 空串 → 全零', () => {
    expect(estimateTokens('')).toEqual({ codePoints: 0, cjk: 0, other: 0, approxTokens: 0 })
  })

  it('CORE-SIZE-02: 401 个 ASCII → ceil(401/4) = 101（向上取整生效）', () => {
    expect(estimateTokens('a'.repeat(401))).toMatchObject({ other: 401, approxTokens: 101 })
  })

  it('CORE-SIZE-03: 20 个汉字 → ceil(20×1.2) = 24（CJK 系数生效）', () => {
    expect(estimateTokens('汉'.repeat(20))).toMatchObject({ cjk: 20, approxTokens: 24 })
  })

  it('CORE-SIZE-04: 非 BMP 按码点计，代理对不虚增', () => {
    const s = 'a\u{1F600}b'
    expect(s.length, '夹具本身必须是 UTF-16 长度 4').toBe(4)
    expect(estimateTokens(s)).toEqual({ codePoints: 3, cjk: 0, other: 3, approxTokens: 1 })
  })

  it('CORE-SIZE-05: 中英混合 = 两系数之和向上取整；全角标点计入 CJK', () => {
    expect(estimateTokens('汉字abc')).toMatchObject({ codePoints: 5, cjk: 2, other: 3, approxTokens: 4 })
    expect(estimateTokens('提示词，包含全角标点。')).toMatchObject({ codePoints: 11, cjk: 11, approxTokens: 14 })
    expect(SIZE_WARN_THRESHOLD).toBe(12_000)
  })

  it('负向：口径不是 chars/4 —— 同一串中文的估算不低于码点数', () => {
    const s = '提示词'
    expect(estimateTokens(s).approxTokens).toBeGreaterThanOrEqual(s.length)
  })
})
```

- [ ] **Step 2: 跑到失败**

Run: `npx vitest run --project unit packages/core/src/pack/size.test.ts`
Expected: FAIL —`Cannot find module './size'`

- [ ] **Step 3: 实现**

创建 `packages/core/src/pack/size.ts`：

```ts
// packages/core/src/pack/size.ts —— 近似上下文量估算（m6a FR-6.2）。
// 系数 1.2 / 4 是**粗估常数**，不是任何 tokenizer 的实测曲线：展示层必须带 ≈ 且措辞为
// 「近似上下文量（非计费口径）」。§14-1 原提议的 chars/4 对 CJK 语料低估 4.0 倍（FR-6.1 实量），
// 所以这里锁死分字区双系数口径；改系数属 B 级变更（会移动 warn 判定线）。
export interface TokenEstimate {
  codePoints: number
  cjk: number
  other: number
  approxTokens: number
}

/** perTarget.approxTokens 超过它 ⇒ warn=true（只提示，永不阻断导出/sync，FR-6.4） */
export const SIZE_WARN_THRESHOLD = 12_000

const CJK_PER_TOKEN = 1.2
const OTHER_PER_TOKEN = 4

/** 假名/汉字/兼容表意/全角标点四个区（FR-6.2 原文区间，不扩也不缩） */
function isCjk(codePoint: number): boolean {
  return (
    (codePoint >= 0x3000 && codePoint <= 0x30ff) ||
    (codePoint >= 0x3400 && codePoint <= 0x9fff) ||
    (codePoint >= 0xf900 && codePoint <= 0xfaff) ||
    (codePoint >= 0xff00 && codePoint <= 0xffef)
  )
}

export function estimateTokens(content: string): TokenEstimate {
  let cjk = 0
  let other = 0
  // Array.from 按码点迭代；用 String.length 会把代理对算两位，emoji 虚增
  for (const ch of Array.from(content)) {
    if (isCjk(ch.codePointAt(0) ?? 0)) cjk += 1
    else other += 1
  }
  return {
    codePoints: cjk + other,
    cjk,
    other,
    approxTokens: Math.ceil(other / OTHER_PER_TOKEN + cjk * CJK_PER_TOKEN),
  }
}

/** 拼接后整体估算（单次 ceil）：perTarget 用它，逐文件相加会随文件数系统性高估 */
export function estimateBundle(parts: readonly string[]): number {
  return estimateTokens(parts.join('')).approxTokens
}
```

- [ ] **Step 4: 出口接线**

`packages/core/src/pack/index.ts` 末尾：

```ts
export { estimateBundle, estimateTokens, SIZE_WARN_THRESHOLD, type TokenEstimate } from './size'
```

`packages/core/src/index.ts` 的 `from './pack'` 段里追加：`estimateTokens,`、`estimateBundle,`、`SIZE_WARN_THRESHOLD,`、`type TokenEstimate,`。

- [ ] **Step 5: 跑到通过**

Run: `npx vitest run --project unit packages/core/src/pack/`
Expected: PASS（CORE-SIZE-01..05 + 既有 pack.test.ts 全绿）

- [ ] **Step 6: 交棒**，等放行。

---

### Task 7: `sizeEstimate` 进 preview（SRV-EST-01..03 = m6a 验收 9、10、11）

**Files:**
- Modify: `packages/shared/src/schemas/pack.ts:120`（`PreviewOut` 加可选字段）
- Modify: `apps/server/src/lib/pack-assemble.ts`
- Modify: `apps/server/test/packs.api.test.ts`（追加一个 describe）

**Interfaces:**
- Consumes: Task 6 的 `estimateTokens` / `estimateBundle` / `SIZE_WARN_THRESHOLD`；`rendered.manifest.targets`；`ADAPTER_MAIN_PATH`
- Produces: `PreviewOut.sizeEstimate?: { perTarget[], footprint }` —— Task 8 的 Web 展示消费它

> `SRV-EST-01b` 是 **Task 6 审查轮引发的计划修订**（2026-09-24）：审查指出「拼接后一次 ceil」与「逐文件 ceil 相加」这两种语义在本计划原先的 SRV-EST 三支里**判别力为零**——`SRV-EST-02` 只跟阈值比（误差方向不敏感），`SRV-EST-03` 只钉确定性。该修订落在 Task 7 而不是回炉 Task 6 的测试文件，理由：判别断言需要真实文件集，而 `size.ts` 是纯函数侧，制造发散夹具的正当场所是组装侧。

- [ ] **Step 1: 加可选字段（shared）**

`packages/shared/src/schemas/pack.ts` 的 `PreviewOut`（:120）之前插入、并在对象里加一行 `sizeEstimate`：

```ts
/** 单文件估算（perTarget 与 footprint 共用形状；path 恒为包内相对路径） */
export const SizeEstimateFile = z.object({
  path: z.string(),
  approxTokens: z.number().int().nonnegative(),
})
export type SizeEstimateFile = z.infer<typeof SizeEstimateFile>

/** 某平台「实际读进上下文」的量：它的主文件 + TERMS.md + CHECKLIST.md + SKILLS.md */
export const SizeEstimatePerTarget = z.object({
  adapter: z.string(),
  files: z.array(SizeEstimateFile),
  approxTokens: z.number().int().nonnegative(),
  warn: z.boolean(),
})
export type SizeEstimatePerTarget = z.infer<typeof SizeEstimatePerTarget>

/** 整包落盘足迹（含被各 adapter 复制的主文件与 manifest）——与 perTarget 是两个视图，不许混成一个数 */
export const SizeEstimateFootprint = z.object({
  files: z.array(SizeEstimateFile),
  approxTokens: z.number().int().nonnegative(),
  bytes: z.number().int().nonnegative(),
})
export type SizeEstimateFootprint = z.infer<typeof SizeEstimateFootprint>

export const SizeEstimate = z.object({
  perTarget: z.array(SizeEstimatePerTarget),
  footprint: SizeEstimateFootprint,
})
export type SizeEstimate = z.infer<typeof SizeEstimate>
```

```ts
export const PreviewOut = z.object({
  files: z.array(PlannedFileOut),
  fingerprint: z.string(),
  warnings: z.array(z.string()),
  coveredPlatforms: z.array(z.string()),
  /** P1.1 / m6a FR-6：可选。**只活在 API 响应里**，不进 manifest 也不进 directoryFiles() */
  sizeEstimate: SizeEstimate.optional(),
})
```

- [ ] **Step 2: 写失败测试**

`apps/server/test/packs.api.test.ts` 末尾追加。顶部 import 补四样：`ADAPTER_MAIN_PATH` from `@openvibe/adapters`、`PACK_FILE_TERMS` from `@openvibe/shared`、`runSeed` 与 `SIZE_WARN_THRESHOLD` from `@openvibe/core`（`SRV-EST-02` 的阈值判断要直接引用它，别在测试里另写字面量 12000）；再加一条种子目录常量（与 `apps/server/test/bootstrap.test.ts:10` **同一算法**，不要另发明路径）：

```ts
import { runSeed } from '@openvibe/core'

const SEED_DIR = join(dirname(fileURLToPath(import.meta.url)), '..', '..', '..', 'content', 'seed')
```

（该文件顶部已有 `mkdtempSync/readFileSync/... from 'node:fs'` 与 `join`；`dirname` 与 `fileURLToPath` 若缺则一并补 `node:path` / `node:url`。）

```ts
describe('SRV-EST · preview 的 sizeEstimate（m6a FR-6 + 验收 9/10/11）', () => {
  it('SRV-EST-01: perTarget 键集合 == manifest.targets；footprint.files 集合 == files[] 集合（含 manifest）', async () => {
    const h = await makeHarness()
    const pack = await mkPack(h) // targets: claude-code + generic-agents
    const out = await preview(h, pack.id, '1.0.0')

    expect(out.sizeEstimate, '响应缺 sizeEstimate').toBeTruthy()
    const est = out.sizeEstimate as NonNullable<typeof out.sizeEstimate>
    expect([...est.perTarget].map((p) => p.adapter).sort()).toEqual(['claude-code', 'generic-agents'])
    expect([...new Set(est.perTarget.map((p) => p.adapter))].sort()).not.toEqual(
      [...out.coveredPlatforms].sort(),
      'perTarget 与 coveredPlatforms 是两套语义（Task 1 ②）：这里断言它们**不相等**，防止有人把断言改回 coveredPlatforms',
    )
    for (const row of est.perTarget) {
      const paths = row.files.map((f) => f.path)
      expect(paths).toContain(ADAPTER_MAIN_PATH[row.adapter as keyof typeof ADAPTER_MAIN_PATH])
      expect(paths).toContain(PACK_FILE_TERMS)
    }
    expect([...est.footprint.files.map((f) => f.path)].sort()).toEqual(
      [...out.files.map((f) => f.path)].sort(),
    )
  })

  it('SRV-EST-01b: 行总量是**拼接后一次 ceil**，不是逐文件 ceil 相加（FR-6.3 的语义差别）', async () => {
    const h = await makeHarness()
    const pack = await mkPack(h)
    const out = await preview(h, pack.id, '1.0.0')
    const est = out.sizeEstimate as NonNullable<typeof out.sizeEstimate>

    // 判别力全在**严格小于**：单 ceil ≤ 逐项 ceil 之和恒成立，所以写成 `<=` 或 `!==` 里挑一个不够——
    // 若实现退回「逐文件相加」，`sum === row.approxTokens`，弱断言照样绿。
    // 反过来，这支红了只有两种原因：① 实现真的是逐文件相加；② 夹具余数恰好对齐（两个文件都无余数）。
    // 二者必须人工分辨并在报告里给出实量数，**不许改成 `<=` 蒙过去**。
    for (const row of est.perTarget) {
      const sum = row.files.reduce((n, f) => n + f.approxTokens, 0)
      expect(row.approxTokens, `perTarget[${row.adapter}] 的单 ceil 总量应严格小于逐文件之和`).toBeLessThan(sum)
    }

    // footprint 同理，且它的文件集含 manifest，发散更明显
    const fpSum = est.footprint.files.reduce((n, f) => n + f.approxTokens, 0)
    expect(est.footprint.approxTokens).toBeLessThan(fpSum)

    // 两条口径不许混成一个数（FR-6.3 原文禁令）：footprint 的文件集是 perTarget 行的**真超集**
    // （多带其它 adapter 的主文件 + manifest），所以只能严格更大，相等即为混用。
    expect(est.footprint.approxTokens).toBeGreaterThan(est.perTarget[0]?.approxTokens ?? 0)
  })

  it('SRV-EST-02: 阈值正负各一支，且 warn 不参与放行（同包导出成功、sync --file 结果不变）', async () => {
    const h = await makeHarness()
    const small = await mkPack(h) // 空库 + 1 条术语 → 远低于阈值
    const smallOut = await preview(h, small.id, '1.0.0')
    expect(smallOut.sizeEstimate?.perTarget.every((p) => p.warn === false), '小包的 perTarget 不该有 warn').toBe(true)

    // 正向支按 m6a §7 验收 10 的**原句**构造：把术语**全选**。空库里没有术语，
    // 所以先跑真种子（`content/seed/terms.json`，现量 109 条），再取全量 id。
    // 这使断言与种子同源——种子审校若显著改变术语体量，这支**应当**变红并逼人重看阈值结论，
    // 而不是靠合成夹具永远绿着。（owner 2026-09-24 裁定：保持「全选」，测试跟着 seed 走。）
    const seeded = runSeed(h.handle.db, SEED_DIR)
    // `SeedBundleResult` 实况（packages/core/src/db/seed.ts:17-24）：`created` 是 **number**、
    // 没有 `.terms` 子对象；状态词是 `status: 'skipped'|'imported'|'error'`。
    expect(
      seeded.some((b) => b.bundle === 'terms' && b.status === 'imported' && b.created > 0),
      '种子术语应真入库',
    ).toBe(true)
    const all = (await api(h, 'GET', '/api/terms')).json() as { items: { id: string }[]; total: number }
    expect(all.total).toBeGreaterThanOrEqual(100)
    const big = await mkPack(h, {
      name: 'all-terms',
      selection: {
        promptIds: [],
        termIds: all.items.map((t) => t.id),
        skillIds: [],
        playbookIds: [],
        flowTemplateId: null,
      },
    })
    const bigOut = await preview(h, big.id, '2.0.0')
    const row = bigOut.sizeEstimate?.perTarget[0]
    // 阈值判断只跟响应自身的数比，不写死任何字面量（反漂移：数字单源在响应里）
    expect(row?.approxTokens ?? 0).toBeGreaterThan(SIZE_WARN_THRESHOLD)
    expect(row?.warn).toBe(true)
    // 这个实量数即 m6a FR-6.4 要的「真 preview 实测」权威值——收口时回填规格，替掉三条字节折算值

    // FR-6.4 的负向断言：超线不改任何放行结果
    const exported = await api(h, 'POST', `/api/packs/${big.id}/export`, { version: '2.0.0', channel: 'download' })
    expect(exported.statusCode, exported.body).toBe(201)
    expect(exported.json().warnings).toEqual([])
  })

  it('SRV-EST-03: 确定性——同一包连续两次 preview，sizeEstimate 序列化后逐字节相等', async () => {
    const h = await makeHarness()
    const pack = await mkPack(h)
    const a = await preview(h, pack.id, '1.0.0')
    const b = await preview(h, pack.id, '1.0.0')
    expect(JSON.stringify(a.sizeEstimate)).toBe(JSON.stringify(b.sizeEstimate))
  })
})
```

- [ ] **Step 3: 跑到失败**

Run: `npx vitest run --project integration apps/server/test/packs.api.test.ts -t 'SRV-EST'`
Expected: FAIL —`响应缺 sizeEstimate`

- [ ] **Step 4: 组装（唯一落点在 `toPreviewOut`）**

`apps/server/src/lib/pack-assemble.ts`：import 段补 `ADAPTER_MAIN_PATH`（已在 :1 从 `@openvibe/adapters` 引 `coveredPlatforms, planMainFiles`，追加一项）、`estimateBundle, estimateTokens, SIZE_WARN_THRESHOLD`（从 `@openvibe/core`）、`PACK_FILE_TERMS, PACK_FILE_CHECKLIST, PACK_FILE_SKILLS, utf8ByteLength, type SizeEstimate, type SizeEstimateFile, type SizeEstimatePerTarget`（从 `@openvibe/shared`）。文件末尾追加：

```ts
/** 某平台真正读进上下文的那批文件：自己的主文件 + 三个辅助产物（存在才算） */
const CONTEXT_AUX = [PACK_FILE_TERMS, PACK_FILE_CHECKLIST, PACK_FILE_SKILLS] as const

export function buildSizeEstimate(
  files: { path: string; content: string }[],
  manifestJson: string,
  targets: readonly AdapterId[],
): SizeEstimate {
  const byPath = new Map(files.map((f) => [f.path, f.content]))
  const perTarget: SizeEstimatePerTarget[] = [...targets]
    .sort(compareCodeUnit)
    .map((adapter) => {
      const paths = [ADAPTER_MAIN_PATH[adapter], ...CONTEXT_AUX].filter((p) => byPath.has(p))
      const rows: SizeEstimateFile[] = paths.map((path) => ({
        path,
        approxTokens: estimateTokens(byPath.get(path) ?? '').approxTokens,
      }))
      const approxTokens = estimateBundle(paths.map((p) => byPath.get(p) ?? ''))
      return { adapter, files: rows, approxTokens, warn: approxTokens > SIZE_WARN_THRESHOLD }
    })

  const all = [...files, { path: PACK_FILE_MANIFEST, content: manifestJson }]
  const footprint: SizeEstimate['footprint'] = {
    files: all
      .map((f) => ({ path: f.path, approxTokens: estimateTokens(f.content).approxTokens }))
      .sort((a, b) => compareCodeUnit(a.path, b.path)),
    approxTokens: estimateBundle(all.map((f) => f.content)),
    bytes: all.reduce((sum, f) => sum + utf8ByteLength(f.content), 0),
  }
  return { perTarget, footprint }
}
```

`toPreviewOut` 的返回对象加一行（`coveredPlatforms` 之后）：

```ts
    sizeEstimate: buildSizeEstimate(
      rendered.files,
      rendered.manifestJson,
      rendered.manifest.targets,
    ),
```

> `AdapterId` 需从 `@openvibe/shared` 一并 import。**注意 `perTarget` 的行集与 `footprint` 的行集不同源**：前者只数该平台读进上下文的文件（不含 manifest，也不含别人的主文件），后者是整包落盘足迹（含 manifest）。把两者算成同一个数就是 FR-6.3 明令禁止的「混成一个数」。

- [ ] **Step 5: 跑到通过**

Run: `npx vitest run --project integration apps/server/test/packs.api.test.ts`
Expected: PASS（SRV-EST-01..03 + 既有 IT-PACK 全绿；`IT-PACK-01` 的「连续两次 preview 全一致」顺带覆盖确定性）

- [ ] **Step 6: 交棒**，等放行。

---

### Task 8: Web 展示（步骤 5 的体量列 + perTarget 成本条 + warn 黄条）

**Files:**
- Modify: `apps/web/src/components/packs/PackPreviewPane.tsx`
- Modify: `apps/web/src/i18n/zh.ts:494-513`（`packs.preview` 块）

**Interfaces:**
- Consumes: Task 7 的 `PreviewOut.sizeEstimate`
- Produces: 步骤 5 的两处显示（m6a §4 步骤5）；无对外接口

- [ ] **Step 1: 文案键**

`apps/web/src/i18n/zh.ts` 的 `packs.preview` 块内追加（措辞受 FR-6.2 的闸：必须带 `≈` 且写「近似上下文量（非计费口径）」，不得写「token 数」）：

```ts
      sizeBasis: '近似上下文量（≈tok，非计费口径）',
      sizeRow: (kB: string, tok: number) => `${String(kB)}kB · ≈${String(tok)} tok`,
      perTargetTitle: '各平台读进上下文的量（主规则文件 + 术语表 + 检查清单 + SKILLS.md）',
      footprintTitle: '整包落盘足迹',
      overBudget: (adapter: string, tok: number) =>
        `${adapter} ≈${String(tok)} 近似上下文量，超过 ${'12000'} 的提示线——只提示，不影响导出与注入`,
```

- [ ] **Step 2: 渲染**

`PackPreviewPane.tsx`：import 补 `import { utf8ByteLength } from '@openvibe/shared'`；文件树每行的 `</button>` 之前追加体量，并在 `coveredPlatforms` 段之后插入成本条与黄条：

```tsx
// 文件树行内（紧跟 <span className="mono block truncate" title={f.path}>{f.path}</span> 之后）
const tokFor = (path: string): number =>
  preview.sizeEstimate?.footprint.files.find((s) => s.path === path)?.approxTokens ?? 0
const sizeFor = (path: string, content: string): string => {
  const kB = (utf8ByteLength(content) / 1024).toFixed(1)
  return zh.packs.preview.sizeRow(kB, tokFor(path))
}
```

```tsx
// 成本条 + 黄条（放在 coveredPlatforms 的 </p> 之后、warnings 段之前）
{preview.sizeEstimate !== undefined && preview.sizeEstimate.perTarget.length > 0 && (
  <div className="space-y-1">
    <p className="text-xs text-zinc-500">
      {`${zh.packs.preview.perTargetTitle} · ${zh.packs.preview.sizeBasis}`}
    </p>
    <ul className="flex flex-wrap gap-2">
      {preview.sizeEstimate.perTarget.map((p) => (
        <li
          key={p.adapter}
          className={`rounded-md border px-2 py-1 text-xs ${
            p.warn ? 'border-amber-300 bg-amber-50 text-amber-800' : 'border-zinc-200 bg-white text-zinc-700'
          }`}
        >
          {`${p.adapter} ≈${String(p.approxTokens)}`}
        </li>
      ))}
    </ul>
    <p className="text-xs text-zinc-500">
      {`${zh.packs.preview.footprintTitle} ≈${String(preview.sizeEstimate.footprint.approxTokens)} · ${preview.sizeEstimate.footprint.files.length} 文件`}
    </p>
    {preview.sizeEstimate.perTarget
      .filter((p) => p.warn)
      .map((p) => (
        <p key={`warn-${p.adapter}`} className="text-xs text-amber-700">
          {zh.packs.preview.overBudget(p.adapter, p.approxTokens)}
        </p>
      ))}
  </div>
)}
```

- [ ] **Step 3: 验证**

Run: `pnpm typecheck` → Expected: 0 error（`sizeEstimate` 是 optional，`!== undefined` 收敛后类型成立）
Run: `pnpm lint` → Expected: 0 问题
Run: `pnpm bundle:check` → Expected: 仍绿；**记下新的入口 kB 数**（基线 291.02 kB / 预算 300 kB），若越线即回 Step 2 删重复渲染而不是提预算

- [ ] **Step 4: 如实登记未验证面**

本仓没有 jsdom/组件测试 project（vitest 三 project 全是 `environment: 'node'`），所以这一步**没有自动化断言**。要么按 owner 待办第 8 项决定接进 CDP 走查（`docs/devlog-evidence/DEV-0019/lazy-chunk-walk.mjs` 是现成骨架），要么在 DEV_LOG 里写「Web 侧显示经 typecheck/bundle 闸，未做 DOM 断言」。不得用「已验证」描述它。

- [ ] **Step 5: 交棒**，等放行。

---

### Task 9: 收口（DoD 五闸 + 文档回填）

**Files:**
- Modify: `docs/tasks.md`（§2b·T10 三条勾选 + 版本行）
- Create: `DEV_LOG.md` 的 `[DEV-0026]` 条目
- Modify: `docs/dev-plan.md`（§15 对应项状态）

- [ ] **Step 1: 五闸 + golden 凭据**

```bash
pnpm lint
pnpm typecheck
pnpm test                       # 三 project 全绿；记下新增用例数与既有基线的差
pnpm seed:check
pnpm bundle:check
git diff --name-only tests/golden/    # 必须为空（m6a 验收 12）
```

Expected: 前五条 rc=0；第六条**无输出**。非空即说明碰了 A 级冻结契约 → 停下回 dev-plan §0.3 走 A 级流程重裁，不要「顺手」更新快照。

- [ ] **Step 2: 三平台口径**

**本步的原始措辞已过期，按实况改写（T3/T4 落地后）**：`clean.test.ts` **确实新增了会在 win32 跳过的支**——
`CLI-CLEAN-06e`（符号链接逃逸）是 `it.skipIf(IS_WINDOWS)`，而「权限位断言本轮没有新增」这半句仍成立。
因此报数时必须分平台写：darwin/linux 是 N 支实跑，win32 是 **N−1 支实跑 + 1 支跳过**，
且**跳过不等于通过**（承 [[openvibe-ci-reading-verdicts]] 的「win32 有 skip 故用例数与 macOS 不等」口径）。
本地仅 darwin 验证，linux/win32 以 CI 为准。

- [ ] **Step 2b: 清掉逐任务裁定留下的三条小项（都是各任务审查时刻意**不**混进修复轮的账）**

1. **`hints` 的承重腿没有断言**（T5 复审指出）。今天唯一钉住 `hints` 的是 `CLI-CLEAN-11` 的普通全清那一轮，
   而那一轮里 `hints` 不承载任何理由；真正「理由只在 hints 里」的三条腿是**交互取消 / `--dry-run` / 陈旧锁接管**
   （`clean.ts:285`、`:309`、`:323-326`），后两条**不需要 PTY**。
   补一支真子进程 `clean <project> --dry-run --json` ⇒ `summary.hints` 含「零写入零删除」，
   让 FR-6.10 v1.5 的必带条款在**它承重的地方**成立，而不是只证到键存在。
2. **`README.md:72-79` 的命令块还缺 `clean` 一行**（T5 审查 Minor-8，`clean` 现已是真命令）。
3. **`clean.ts:300-301` 的文案对**（T5 审查 Minor-7）：`--force` 且 DRIFT 为 0 时读作「连已改动的 **0** 个一起删」。
   要改就**连带既有孪生句 `:301` 一起改**（`counts.DRIFT > 0` 守卫），别只改新句留下两种口径。
4. 另有两条**裁定为未验证面**、不得自造证据的：交互取消走真子进程需 PTY；`--json` 轨渲染违规项属 T4 面。
   这两条进步骤 4 的「未验证面」清单。

- [ ] **Step 3: 回填 `docs/tasks.md` §2b·T10**

`clean` 与 `预览体量与 token 估算` 两条 `- [ ]` → `- [x]`，各带一句结论 + 用例数 + 提交号占位（等 owner 放行 commit 后补 sha）；`一项目一包边界声明` 一条改为 `- [x] …… 以 Task 1 §6.10 修正收口（实现未动，规格随实况）`。验收行补上 `git diff --name-only tests/golden/` 的**空输出**这一条凭据。

- [ ] **Step 4: DEV_LOG 八段式登记（`[DEV-0027]`——`[DEV-0026]` 已被本轮「计划落库 + Task 1 三处冲突定稿」占用）**

登记时**引用 DEV-0026 的冲突裁定**而不是复述一遍（数字与结论单源），本条只写实现侧新证据。

必写清的四点，按本次实测填数：
1. 三条规格冲突 + 证据（那两行实跑输出、`registry.ts:65`/`compat.ts` 的行号、`lock.ts:36` 的并集规则）与其带来的验收改道；
2. `CLI-CLEAN-*` 的**实际支数**——登记时以 `grep -c "^\s*it[\.(]" apps/cli/test/clean.test.ts` 与 vitest 汇总行现测为准，**不要抄任何历史数字**（本计划写在 T4 之前的「17 支」到 T4 落地时就已过期一次）。口径要求不变：**不得写成「规格要求的十支全绿」**，因为实落里有大量规格没点名的 sub-branch。截至 T4 修复轮 1 的实际清单与归属（§7.10 括号内）：
   - 规格点名的 a–j：`01`(a) `02`(b) `03`(c) `04`(d) `05`(e) `05b`(e2) `06b`(e3) `06`(f) `06c`(f2) `07`(g) `08`(h) `09`(i，T5) `10`(j，T5) `11`/`12`(命令注册与 `--json` 契约，T5)；
   - 规格未点名、实施/审查补的：`01b` 退场后 diff 报未注入、`04b` 无 lock 的 dry-run 零副作用、`04c` dry-run 不改退出码、`05c` 真走 `confirmWithClack` 的 accept/cancel 两腿、`05d` `Ctrl-C` 哨兵与答「不」同口径、`06d` 全 FOREIGN 也照样收尾删 lock、`06e` 符号链接逃逸（**win32 上 `skipIf`，属未验证面**）、`06f` 重复登记项按条目计数不中止、`08b` 零 removal 但删 lock 时同样受锁保护（T4 修复轮 R-1）、以及 f2 的「非 JSON」腿（Minor-1）；
   - `06b` 的标题在 v1.4 判据变更后已改为「全部 ABSENT」，全 FOREIGN 那支是 `06d`——旧版本这一行把两者写反了，登记时按上面的清单走。
3. ~~SRV-EST-02 的实量 `approxTokens` 回填 FR-6.4~~ **✅ 已在本计划 Task 7 收口时落地（m6a v1.3，owner 2026-09-24 批准），Step 9 只核对不重做**。落地后的口径三条，登记时逐条对齐：
   - **权威数是 `SRV-EST-04` 的预置包实测，不是 `SRV-EST-02` 的全选夹具实测**：FR-6.4 那句「首启预置包自己就会亮黄条」说的是 default 包，所以进规格的是 `runSeed` + `ensureDefaultPack` 那条分支的量（claude-code/codebuddy/generic-agents/minicode 15,213、cursor 15,235、trae 15,232，footprint 67,621 tok / 186,365 B）；`SRV-EST-02` 的 `all-seed` 夹具（现测 14,890）只是**阈值正负两支**的正向支，随包名长度挪 1 tok，故两个数都**不进断言字面量**。
   - **折算三档不是「被实测取代后作废」，而是它的归因错了**：v1.2 及以前那三条（≈15.1k/15.7k/16.0k）字节基数用的是**渲染后的** `TERMS.md`（实测 25,472 B），借错的只是 **CJK 占比那一维**（0.60/0.784/0.926 来自全字段/子集语料口径，渲染表自身占比 ≈63%）。实测 15,213 恰好落在第一档附近，所以**结论一字未动**——但「口径要与数字同处登记」这条教训因此多了第二维，已写进 FR-6.1 的口径注。
   - **owner 裁定的种子同源仍然生效**：正向支走真种子，断言与种子同源；种子审校若把体量压到 12,000 以下，`SRV-EST-02` 与 `SRV-EST-04` **都应当变红**并逼人重看阈值，而不是靠合成夹具绿着。
4. **验收 10 的构造由「术语全选」改为「资产全选」**（m6a v1.3）：原句经实测否证——真种子 109 条术语全选、提示词为空时 `perTarget[claude-code]` 只有 **9,617 tok < 12,000**，因 `TERMS.md` 只渲染 `zh`/`en`/`aliases`/`definition` 四列，`example`/`tags` 从不落盘，而原句据以推断体量的 14,622 码点是**全字段**口径。**登记时必须写成「规格被实测否证后由 owner 裁定改写」，不得写成「实现满足了验收 10」**——阈值与系数一字未动，动的构造。
5. **§7 验收 10 的 sync 腿属结构性保证，不是被证明的命题**：`sizeEstimate` 既不进 `bundleJson()` 也不进 `directoryFiles()`（`apps/server/src/routes/packs.ts:91,98`），CLI 无从观察它，故 server 侧**没有**、也不该有子进程级断言。Task 9 的「未验证面」清单必须收这一条（T7 审查 I-4，修复轮已在 `it()` 标题与注释里去掉谎报）。
6. **Task 8 落笔前的一个待决类型收窄**：`SizeEstimatePerTarget.adapter` 现是 `z.string()`（T7 审查 Minor-5，controller 裁定推迟到 Task 8 决定），而按 target 渲染成本条要的是 `AdapterId`。收窄会改推断类型并波及三个 type-only 消费点，**先看全消费点再动**。
7. 未验证面（逐条点名，不写「win32 未见 CI」这种一句话盖过的模糊话）：**Web 显示无 DOM 断言**（Step 8-4，owner 已裁定不引入组件测试基建）；**`CLI-CLEAN-06e` 符号链接逃逸支在 win32 上是 `skipIf(IS_WINDOWS)`——跳过 ≠ 通过**；**`auditLockPaths` 的 win32 词法子句（`C:/evil.txt` 过得了 `isValidPackRelativePath`、只有 `path.win32.resolve` 会把它归到根外）在三平台 CI 上都没有可执行证据**（T4 审查 Minor-3，controller 裁定保留代码 + 登记，不加 `vi.mock('node:path')` 假证据）；`PATH_ESCAPE` 的删除循环兜底支在无端到端构造（需 planning 与 unlink 之间换盘），只有 core 侧谓词单测；`clean` 的中断恢复（删一半失败）只有单测覆盖，未做真 SIGKILL 演练。

- [ ] **Step 5: 交棒给 owner（不自行 commit）**

一次性交出：`git status --porcelain` 全清单、五闸输出、`git diff --name-only tests/golden/` 的空输出、Task 1 的规格 diff、新增用例数对比。owner 待办随之更新——P1.1 的 T10 从「规格待复核」变成「实现待放行」，真发布 / 推 tag / 种子审校三项仍在 owner 手上。

---

## 自审（写完整份计划后回查，结果已内联修正）

**1. 规格覆盖**：m6b FR-6.1–6.10 → Task 2/3/4/5（三态与 `managed` 闸 / 备份 / lock 与备份去留 / 净化 / 交互 / 并发 / 退出码 / `--json` 契约）；§6.10 一项目一包 → Task 1 Step 2 + Task 9 Step 3（无实现项，按实况修文）；验收 §7.10 a–j + f2 → `CLI-CLEAN-01..10` + `06c` 一一映射，另多出六支 sub-branch（`01b`/`04b`/`05b`/`06b`/`11`/`12`）属契约与前置支。m6a FR-6.1–6.7 → Task 6（函数与口径）/ Task 7（两视图、可选、不阻断、零新依赖、确定性、冻结凭据）/ Task 8（展示层措辞）；验收 8–12 → CORE-SIZE / SRV-EST-01 / SRV-EST-02 / SRV-EST-03 / Task 9 Step 1。**Task 1 状态：✅ 已完成（2026-09-24）**——三条冲突 + 三处同源涟漪 + 一处规格空白已落 `m6b v1.3` / `m6a v1.2`，登记见 `DEV_LOG [DEV-0026]`，`tasks.md §2b·T10` 加了一行开工前置。**未覆盖并已知**：T11 的发布传播清单（前置是 v0.1.0 真发布，在 owner 手上）、`@import` 受管子文件（S-1 结论=落地归 P2）。

**2. 占位符扫描**：无 TBD / 「类似 Task N」；Task 4 Step 1 里故意留了一处**反面样本**（对靠 stdin 持锁的夹具误用 `spawnSync` 会挂住测试），附了「写测试时删掉那三行」的指令与保留它的理由——它是教学用的陷阱，不是待填项。

**3. 类型一致性**：`planRetirement` / `RetirementPlan` / `RetirementState` / `RetirementAction`（Task 2 定义 → Task 3/5 消费）、`cleanAction` / `CleanOutcome` / `CleanSummary` / `CleanReportRow` / `CleanError`（Task 3 定义 → Task 5 渲染）、`estimateTokens` / `estimateBundle` / `SIZE_WARN_THRESHOLD`（Task 6 → Task 7 与测试）、`SizeEstimate` 系 schema 类型（Task 7 shared → Task 7 assemble 与 Task 8 Web）。Task 3 的 `CleanSummary` 含 `foreign` 一键，FR-6.10 的六键之外——已核对：`report[].state` 已能表达 FOREIGN，多出的键属**附加**不属改名，Task 5 的 `Object.hasOwn` 断言按 FR-6.10 的六个必存在键写，不因此破裂。
