# OpenVibe v0.1.0 · 发布说明

> 首个可安装版本。包名 `openvibe-cli`（npm 上 `openvibe` 已被占用），命令名 `openvibe`。

## 装与用

```bash
npx openvibe-cli serve --open                              # 起本地服务并开浏览器
npx openvibe-cli sync ~/your-project --pack default        # 把预置标准包注入项目
npx openvibe-cli diff ~/your-project                       # 查漂移（0=一致 / 2=有改动）
```

要求 Node ≥ 22。全部数据在 `~/.openvibe/`（`data/openvibe.db` + `config.json` + `packs/`），
运行期不需要外网；项目侧只多一个 `.openvibe/` 台账目录与注入出的规则文件。

## 这一版有什么

- **五类资产本地库**：提示词 / 术语 / Skill 台账 / 流程模板 / 标准包，109 条术语、3 套流程模板、
  20 条精选提示词随包播种，首启即有一个可用的 `default` 标准包。
- **标准包注入六个助手**：`CLAUDE.md`、`.cursor/rules/openvibe.mdc`、`AGENTS.md`、`CODEBUDDY.md`、
  `.trae/rules/openvibe.md`、`MINI.md`，外加 `TERMS.md` / `CHECKLIST.md` 与 `.openvibe/pack.lock.json` 台账。
  同一份内容指纹，跨助手一致。
- **漂移看得见**：`diff` 报 `DRIFT`，`sync` 冲突三选一（以包为准 / 保留本地 / 跳过），
  任何覆盖前先写 `.openvibe/backup/`；`--dry-run` 承诺零写入。
- **流程与回流**：项目工作台按模板推进、记开发日志，日志可一键生成术语/提示词草稿并挂来源反链 ——
  飞轮第⑤步是真实端点，不是路线图。
- **Web 九页 + CLI 四命令**（serve / sync / diff / scan），`--json` 契约统一，退出码 0/1/2。
- **遥测默认关**：白名单三类事件、五段载荷、不含路径与内容；未配置端点时连上报定时器都不创建。

## 测量口径（不是形容词，是数字）

| 项 | 实测值 | 出处 |
|---|---|---|
| 测试 | 381 用例 / 44 文件（unit + integration + cli 三层） | `pnpm test` |
| 发布形态断言 | 45 项全 PASS / 0 FAIL | `docs/devlog-evidence/DEV-0020/publish-walk.txt` |
| tarball | 1.15 MB / 36 个文件 / 解包 4.9 MB | `npm publish --dry-run` + 同上走查 |
| 产物入口 | `dist/cli.js` 3.57 MB（562 模块内联，0 条 esbuild 警告） | `pnpm pkg:cli` |
| Web 入口 chunk | 291 kB（闸门 ≤300 kB），懒加载 23 块 | `pnpm bundle:check` |
| 用户侧计时 | 4.3 s（serve → sync → diff 三连，干净沙箱） | 同上走查 |
| 构建+pack+装包 | 27.6 s，其中装包 4.1 s（含两次真装包探针） | 同上走查 |

计时是瞬时量，同路径两轮实测为 **用户侧 1.0–4.3 s / 构建+pack+装包 27.6–52.0 s**（差别全在装包段，
即预编译包的下载量），别把某一轮的秒数当契约读。

## 已知局限（装之前就该知道）

- **一项目一包**：`pack.lock.json` 是单包结构，一个目录同时只由一个标准包托管，换包即整包替换，不做多包叠加。
- **整文件管理，无块级合并**：受管单位是整个产物文件。资产从包里移除后不会自动清理旧产物里那段内容；
  没有 `update` / `remove` 子命令，回滚请用 `.openvibe/backup/`。
- **无云同步与账号体系**：团队协作靠 bundle JSON 走 git（导出 → 提交 → 别人 `sync --file`）；
  权限、审批、LLM 密钥托管在 P2/P3。
- **中文 UI 优先**，暂无英文版；种子内容以中文技术术语为主。
- **pnpm 装包需要一次 `approve-builds`**：pnpm 10 默认拒跑依赖安装脚本，会先报
  `ERR_PNPM_IGNORED_BUILDS`（安装器语义，不是打包缺陷）。npm / npx 用户按平台取预编译包，不涉及这一步。

## 本版本**未**验证的事（按未验证处理，不当通过）

- 干净环境走查在 **macOS + Node v26（ABI node-v147）** 上跑通预编译包；
  Windows / Linux 与非 147 ABI 的**装包路径**没有本机证据 —— 三平台 CI 覆盖的是源码构建与五道门禁
  （Lint / Typecheck / Test / Seed check / Bundle check，Node 22），不含 `npm pack` 之后的产物路径。
- **源码编译回退**（`node-gyp rebuild`）在本机无法证明：该机的 C++ 工具链缺头文件，
  `#include <climits>` 直接失败。取不到预编译包的用户仍未验。
- 真实 `npm publish` 尚未执行：本地 npm 与 pnpm 的 registry 均指向 `registry.npmmirror.com`，
  两次 dry-run 都打出「Publishing to https://registry.npmmirror.com」—— 真发布必须显式
  `--registry=https://registry.npmjs.org`。
- 复制按钮、中文输入搜索、看板拖拽三处交互证据属**人工录屏/截图**，自动化不含
  （合成点击拿不到 transient user activation，自动化里必然被拒）。

## 维护者：发布三步

```bash
pnpm pkg:cli                        # 生成 apps/cli/pkg/（暂存清单已是 npm 归一化后的形态）
cd apps/cli/pkg && npm publish --registry=https://registry.npmjs.org
gh release create v0.1.0 --title "OpenVibe v0.1.0" --notes-file docs/release-notes/v0.1.0.md
```

清单里 `files: ["dist"]` 之外只有 LICENSE / README / package.json —— 上面那次 dry-run 的 36 个文件已逐条核对，
没有 `node_modules`、`src` 或 `.openvibe/` 混进去。
发包前复跑 `node docs/devlog-evidence/DEV-0020/publish-walk.mjs`，45 项断言须 0 FAIL。
