# OpenVibe（灵典）产品需求文档（PRD）

| 项 | 值 |
|------|------|
| 产品名 | **OpenVibe** / 灵典 |
| 版本 | v0.1.7（2026-09-24，**三条冒烟收口回填**，裁定 D19–D22 未变；上一版 v0.1.6 = 2026-09-23 S-2 探针结论回填；v0.1.5 = 同日 P1.1 开档 D19–D22；v0.1.4 = 2026-09-21 D17 更名 OpenVibe） |
| 状态 | MVP 部分**已定稿**——P0 全部关闭（含 A8 包名核验、MiniCode/Trae/Kimi 契约核验、工期 6.5 周、向导自动确认、遥测端点定案）；2026-09-20 评审修订 D13–D16；2026-09-21 D17 更名；**2026-09-23 D19–D22 开出 P1.1 迭代版**（范围裁定 + `clean` 删除边界 + 冒烟证据口径 + M4 归 Phase 2 勘误），P1.1 细则见 [dev-plan.md §15](./dev-plan.md) |
| 文档结构 | 按 prd-iterative 技能 9 章规范产出 |

---

## 0. 需求澄清与决策记录

> v0.1 在自主模式下产出，7 项假设待确认。**2026-09-20 owner 两轮沟通**（brainstorming 澄清 + 裁定收口，记录见附录 D）逐项过完：D1–D7 全部定案（含契约冻结与国内平台扩充）。用户原始需求不变：**五大管理对象**（提示词、Skill、vibe 术语、技巧、coding 项目管理流程）+ **标准化流程管理工作台**定性。

| # | 维度 | 结论 | 状态与来源 |
|---|------|------|-----------|
| 1 | 用户画像 | **中文社区开源优先**：产品按社区受众打磨（README/新手向导/冷启动内容），起步服务个人，架构预留小团队 | ✅ 定案（D1） |
| 2 | 技术架构 | 自部署 Web + 本地 CLI（注入唯一物理通道，硬依赖）；本地优先 | ✅ 沿用默认确认 |
| 3 | 产品形态 | MVP = Web SPA + CLI；P2 评估桌面端/浏览器扩展 | ✅ 沿用默认确认 |
| 4 | 数据来源 | 手动录入 + 本地扫描/规则文件反向导入；生态导入 P1 | ✅ 沿用默认确认 |
| 5 | 成员粒度 | MVP 单用户；Phase 2 多用户与角色 | ✅ 沿用默认确认 |
| 6 | 合规定位 | 无特殊合规；敏感 Key 本地 AES-256-GCM 加密不外传；**遥测改为可选匿名统计：默认关闭、事件清单公开、可随时关** | ✅ 定案（D3，改写原「无遥测」表述） |
| 7 | 模块边界 | 五大对象 → 七模块；MVP = 3 核心 + 2 瘦身（与双钩子配重一致，见 1.3 权重） | ✅ 定案（D2 确认原收敛） |
| 8 | 开箱体验 | **预置演示标准包 + 可跳过的 ≤3 步首启向导**（见 3.4） | ✅ 定案（D4） |
| 9 | 平台支持 | **macOS / Linux / Windows 三平台为硬要求**；CI 三平台矩阵 | ✅ 定案（D5） |
| 10 | 开源协议 | 代码 Apache-2.0，种子内容随代码同协议发布 | ✅ 定案（D6，2026-09-20 二次裁定） |
| 11 | 平台覆盖 | adapter 覆盖国内主流平台，**专用 adapter 六个**：claude-code / cursor / generic-agents / **codebuddy（CODEBUDDY.md）/ trae（.trae/rules/openvibe.md，v1.2）/ minicode（MINI.md）**；zcode / Kimi Code 等经兼容矩阵走 AGENTS.md 产物（见 design §8 v1.2） | ✅ 定案（D7/D9/D10，全部经核验；Kimi 真机抽测挂 T6） |
| 12 | 遥测增长 | 首次 `sync` 注入成功后**一次性 opt-in 询问**（CLI 一行提示 + Web 向导③完成处卡片；默认关、拒绝后本地标记永不再问；事件白名单不变） | ✅ 定案（D13，2026-09-20 评审对话） |
| 13 | 内容产能 | 0.5 内容编辑并行轨取消：**AI 起草（词条/提示词初稿）+ owner 审校 ≈ 2-3 人日**；审校不达标按变体③ 60 条首发、seed 幂等热补至 100；6.5 周维持 | ✅ 定案（D14，2026-09-20 评审对话） |
| 14 | 项目命名 | **更名 OpenVibe**（中文名沿用灵典）；npm `openvibe` 被占 → **包名与冷启动命令 `openvibe-cli`**（registry 404 实测 2026-09-21），bin 命令 `openvibe`；契约字符串随 design §7/§8 v1.3 更新（结构不变） | ✅ 定案（D17，2026-09-21） |

---

## 第 1 章 · 产品概述与目标

### 1.1 一句话定位

> **OpenVibe 是 vibe coding 的标准化工作台：把提示词、Skill、术语、技巧和项目流程变成可管理、可复用、可分发的工程标准。**

### 1.2 背景与问题

vibe coding（用自然语言与 AI 结对完成编码）已成为主流开发方式，但配套的「工程化」严重缺位：

1. **资产散落**：提示词躺在聊天记录和收藏夹里，规则散落在 `.cursorrules` / `CLAUDE.md` / `AGENTS.md`，换项目、换工具、换人即失效；
2. **没有标准**：同一个团队每个人喂给 AI 的规则质量天差地别，AI 输出质量随人而变；
3. **知识流失**：踩过的坑（技巧）、发明过的词（术语）没有沉淀载体，新人重新踩一遍；
4. **流程随意**：AI 编码项目缺少统一的需求→实现→复盘流程，质量靠运气。

现有工具各占一角（详见 `competitive-research.md`）：PromptHub 管个人资产、BMAD/spec-kit 管流程模板、skills.sh 管 skill 分发——**无人做「资产 + 知识 + 流程」的标准化闭环**。

### 1.3 核心目标（3 个）

| # | 目标 | 衡量方式 |
|---|------|----------|
| G1 | **资产标准化**：五类资产一处沉淀、版本化、多平台一键分发（Claude Code / Cursor / Windsurf / Codex 等） | 标准包分发次数；资产复用率（被引用次数/总量） |
| G2 | **流程标准化**：AI 编码项目从启动→开发→复盘有模板、有检查清单、有日志 | 按模板执行的项目占比；复盘产出条数 |
| G3 | **知识标准化**：术语统一（团队说同一种语言）、技巧结构化沉淀（坑不重踩） | 术语库覆盖率（会话高频词入库率）；技巧库月新增 |

> **权重定案（2026-09-20 澄清 D2）**：G1 + G2 为主目标——社区传播**双钩子** = 标准包一键注入（机制独特性）+ 可视化流程模板（对比 BMAD 只发文件，我们是可裁剪工作台）；G3 为支撑目标，术语以 TERMS.md 随包分发出场，不独立主打。

### 1.4 成功指标（MVP 发布后 3 个月；2026-09-20 改双层口径）

**遥测层（下界口径）**——仅统计主动开启匿名遥测的用户，代表真实量的下界：
- 标准包注入 ≥ 100 次；周活跃项目 ≥ 10；按模板执行的项目占比 ≥ 50%
- 提示词复用率 ≥ 30%（产品内面板口径）；用户自建词条 ≥ 50

**生态层（代理指标）**——本地优先产品的公开健康度信号：
- GitHub stars ≥ 300；npm 月下载 ≥ 500（建议值，owner 可调）

**内容层**：术语库预置词条 ≥ 100（全量目标；v0.1.0 允许 60 条首发、经 seed 幂等热补至 100——D14 审校进度决定，见 specs/seed-content.md 与 tasks §3 变体③）

**北极星指标**：**「标准化飞轮完整走完的圈数」**（沉淀资产 → 组装标准包 → 注入项目 → 复盘回流新资产）。落地形态 = 产品内用户可见的**飞轮统计面板**（留存玩法，本地统计不上报，见 specs/onboarding.md）。

---

## 第 2 章 · 用户画像与场景

### 2.1 用户画像

| 画像 | 描述 | 核心诉求 | 优先级 |
|------|------|----------|--------|
| 独立 vibe coder | 资深全栈开发者，同时用 2-5 个 AI 编码工具 | 资产多平台同步，不再翻聊天记录找提示词 | P0 |
| 团队 Tech Lead | 1-10 人团队的负责人 | 给团队定一套 AI 编码规范并强制落地 | P1 |
| AI 编码新手 | 刚入坑 vibe coding 的开发者 | 学术语、抄技巧、照流程走，少走弯路 | P1 |
| 社区新用户 | 开源发布后的首次安装者（D1 新增） | 3 条命令 / 5 分钟内体验「注入 + 流程模板」双钩子 | P0 |

### 2.2 核心使用场景（用户故事）

1. **作为独立开发者**，我希望把散落在各项目的提示词和规则文件统一导入管理，并在新项目启动时一键注入，以便 10 分钟完成过去 2 小时的「配环境」。
2. **作为 Tech Lead**，我希望定义团队的标准开发流程（需求→PRD→实现→复盘）与各阶段检查清单，并打包成「标准包」分发给所有成员的项目，以便团队 AI 输出质量有统一底线。
3. **作为团队成员**，我遇到「AI 改了不该改的代码」「规则漂移」等问题时，希望能查到统一术语的定义和对应的应对技巧，以便与 AI 高效沟通。
4. **作为 Skill 收集者**，我希望在一个地方管理 skill 并分发到我用的所有编码代理，以便统一升级、避免版本混乱。
5. **作为新手**，我希望通过术语库 + 技巧库 + 流程模板快速上手 vibe coding 方法论，以便直接站在团队经验上工作。

### 2.3 使用频率预估

| 频率 | 场景 |
|------|------|
| 每天 | 项目工作台（看板、检查清单、复制提示词、记日志） |
| 每周 | 资产整理（打标签、归档、review 新技巧） |
| 事件驱动 | 新项目启动（注入标准包）、项目复盘（CHECK 报告、技巧回流） |

---

## 第 3 章 · 功能模块设计

### 3.1 模块总览

```
┌─────────────────────────── OpenVibe ───────────────────────────┐
│  资产层   M1 提示词库   M2 Skill 中心   M3 术语库   M4 技巧库     │
│  流程层   M5 项目与流程管理（模板/看板/检查清单/开发日志）          │
│  交付层   M6 标准包与注入（标准化出口，Web↔CLI↔目标项目）          │
│  协作层   M7 团队协作（Phase 2）                                  │
└──────────────────────────────────────────────────────────────────┘
```

依赖关系：M1–M4（素材）→ M6（打包分发）；M5 ↔ M6（项目引用标准包版本）；M7 横切所有模块。

### 3.2 模块明细

#### M1 提示词库（P0，MVP 核心）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 提示词 CRUD | Markdown 编辑 + 代码高亮 + 实时预览 | P0 |
| 模板变量 | `{{variable}}` 占位，复制时弹表单填值 | P0 |
| 组织与检索 | 标签 + 文件夹 + 全文搜索（FTS5） | P0 |
| 版本管理 | 每次保存自动快照，版本对比（diff 高亮）与回滚 | P0 |
| 平台适配标记 | 标记适用平台（Claude Code / Cursor / CodeBuddy / 通用…），同一定义多平台变体 | P0 |
| 导入导出 | Markdown / JSON 互导；从 `.cursorrules`、`CLAUDE.md` 反向导入 | P0 |
| AI 测试场 | 多模型并行对比试跑 | P1（Phase 3） |

- **输入**：手动创建、文件导入、复盘回流
- **输出**：标准包素材（M6）、复制即用

#### M2 Skill 中心（P0 瘦身版 → P1 完整版）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 本地扫描发现 | 扫描 `~/.claude/skills`、`.claude/skills` 等目录，自动登记已有 SKILL.md | P0 |
| 手动登记与版本记录 | 手动添加 skill 条目、记录版本与来源 | P0 |
| 生态源导入 | 对接 GitHub / skills.sh / 本地目录作为 skill 源，浏览安装 | P1 |
| 多平台分发 | 一键分发到 Claude Code / Cursor / Windsurf / Codex / CodeBuddy / Trae / MiniCode 等，symlink / copy 双模式 | P1 |
| 冲突与更新检测 | SHA-256 指纹比对同名 skill，冲突生成变体；源更新提醒 | P1 |

- **输入**：本地目录扫描、源拉取、手动
- **输出**：标准包素材；经 CLI 写入目标平台目录

#### M3 术语库（P0 瘦身版 —— 差异化模块）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 词条 CRUD | 字段：中文名 / 英文名 / 别名[] / 定义 / 示例 / 相关术语 / 来源 / 标签 | P0 |
| 预置种子词条 | 内置 ≥100 条 vibe coding 高频术语（见附录 B） | P0 |
| 全文搜索 | 中英文 / 别名均可命中 | P0 |
| 生成术语片段 | 一键将选中的词条集生成 `TERMS.md`（供标准包注入） | P0 |
| 社区提案 | 词条的新增/修改走提案-审核流（Phase 2 团队版） | P1 |

- **输入**：预置数据、手动、复盘回流（新词候选）
- **输出**：TERMS.md 素材（M6）、检索服务

> 审查修正说明：术语「注入 AI 上下文」统一经由 M6 标准包 + CLI 完成，M3 自身只产出 Markdown 素材——Web 版无法直接写本地项目文件（见第 9 章审查记录）。
>
> **澄清补充（2026-09-20，D2）**：MVP 中 M3 的角色 = 随标准包分发的知识支撑（TERMS.md），**不作为独立主传播钩子**——社区主找示例注入机制与流程模板；词条库的独立检索价值作为次要入口保留。

#### M4 技巧库（P1，Phase 2）

> **D22 勘误（2026-09-23）**：本模块标题原标「（P1）」，与 3.3「M2 完整版、M4、M7 进 Phase 2」及第 8 章 P2 行冲突——**以 Phase 归属为准改判 Phase 2**，写法沿用 M7 的「（P1，Phase 2）」惯例；下表三行的 P1 是模块**内部功能相对优先级**，不构成进 MVP 的承诺（原「（P1）」的读法正是歧义来源）。同时登记口径缺口：`docs/specs/` 无 m4 规格、`tasks.md`/`dev-plan.md` 全文零次提及 M4——既未实现也此前未被显式裁掉，进入实施前须先补 spec（dev-plan §0.4「No spec, no task」）。

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 技巧条目 | 结构：场景 + 问题 + 正确做法 + 反例 + 关联资产（提示词/skill/术语链接） | P1 |
| 复盘草稿生成 | 从项目复盘日志中提取候选技巧，人工确认入库 | P1 |
| 实战标记与评分 | 标记「亲测有效」，记录使用证据 | P1 |

- **输入**：手动、M5 复盘回流
- **输出**：标准包素材、检索服务

#### M5 项目与流程管理（P0，MVP 核心）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 项目工作台 | 项目列表 + 健康摘要（所处阶段、未完检查项、最近日志） | P0 |
| 流程模板 | 内置：① 个人轻量流（启动→开发→复盘）② Spec 驱动流（裁剪自 spec-kit/BMAD 思路：需求→PRD→Spec→计划→实现→审查→复盘）③ 复盘流；支持复制模板后自定义 | P0 |
| 阶段与检查清单 | 每阶段可挂检查清单（如「实现前：上下文文件已注入」「合并前：测试通过」） | P0 |
| 任务看板 | 简化 kanban（待办/进行/完成），任务挂到阶段 | P0 |
| 开发日志 | 内置 DEV-LOG / CHECK-LOG 模板（兼容「编号递增 + 时间戳 + 关联文件 + 验证记录」规范，见附录 C），手动 + 模板生成 | P0 |
| 会话记录挂接 | 手动粘贴 AI 会话摘录作为日志附件 | P1 |

- **输入**：模板实例化、手动操作
- **输出**：项目标准文件（经 M6 注入）、复盘记录 → M4/M1/M3 回流

#### M6 标准包与注入（P0，MVP 核心 —— 「标准化」的物理载体）

| 功能 | 说明 | 优先级 |
|------|------|--------|
| 标准包组装 | 从 M1–M4 挑选资产 + 选定流程模板 → 生成带版本号的 StandardPack | P0 |
| 文件生成 | 目标产物为纯 Markdown/YAML：`CLAUDE.md` / `AGENTS.md` / `CODEBUDDY.md` / `MINI.md` / `.cursor/rules/*.mdc` / `.trae/rules/openvibe.md` / `TERMS.md` / skills 清单 / 流程 CHECKLIST.md —— git 友好、工具无关；zcode/Kimi Code 等经兼容矩阵由 `AGENTS.md` 覆盖（D7/D9/D10，2026-09-20） | P0 |
| CLI 注入 | `openvibe sync <project-path>`：写入目标项目，带 dry-run 预览 | P0 |
| 冲突保护 | 目标已有同名文件 → diff 预览 + 自动备份 + 用户确认后才覆盖 | P0 |
| 版本追踪 | 记录注入指纹，重跑时检测漂移 | P0 |
| **受管文件退场**（D19/D20，P1.1 新增） | `openvibe clean <project-path>`：删除 lock 登记且**未被改动**的受管文件，删前一律备份；被用户改过的默认保留（需显式 `--force`），**非受管文件任何情况下不删**。规格 `specs/m6-cli-injection.md` FR-6 | P1.1 |
| **预览体量与 token 估算**（D19，P1.1 新增） | 组包向导第 5 步按目标平台显示 kB 与 ≈token（中文感知系数，非 `chars/4`），超阈值只亮黄条不阻断。规格 `specs/m6-standard-pack.md` FR-6 | P1.1 |
| 双向同步 | 项目内改动回读比对（以本地为准/以标准包为准可选） | P1 |

- **输入**：资产层素材
- **输出**：目标项目目录中的标准文件 —— **这是全产品的价值出口**

#### M7 团队协作（P1，Phase 2）

- 多用户与角色（owner / member / reviewer）
- 提示词与 skill 变更走 PR 式评审（先审后入团队标准）
- 团队共享库与成员使用统计

### 3.3 MVP 边界（经第一性原理审查收敛）

**MVP = M1 完整 + M5 完整 + M6 完整 + M3 瘦身（预置+CRUD+生成 TERMS.md）+ M2 瘦身（扫描+登记）**；M2 完整版、M4、M7 进 Phase 2。

### 3.4 开箱体验（2026-09-20 澄清新增，D4）

| 能力 | 说明 | 优先级 |
|------|------|--------|
| 预置演示标准包 | 首启时由种子内容自动组装 1 个 `default` 标准包（精选提示词 + 术语 + 个人轻量流模板），开箱即可 `openvibe sync` 到测试项目 | P0 |
| 首启向导 | Web 首启触发，**可跳过，≤3 步**：①认识资产库（种子已就位）→ ②预览预置包组成 → ③复制 sync 命令注入测试目录并跑一次 diff；**每步产生真实副作用，不做纯展示动画** | P0 |
| 飞轮统计面板 | 工作台常驻卡片：资产数、组包数、注入次数、回流条数、完整飞轮圈数（本地统计，不上报，支撑北极星观测） | P0 |

**验收线**：新用户从 `npx openvibe-cli serve` 到在自己目录看到注入产物，≤3 条命令、≤5 分钟（详细规格见 specs/onboarding.md）。

---

## 第 4 章 · 系统架构设计

> **[假设]** 技术选型以下述为默认方案，owner 可在评审时替换；「Web + CLI 伴侣」的双形态不是假设而是审查修正的硬约束（见第 9 章）。

### 4.1 架构图（文字）

```
┌─ Web UI (SPA: React + TS + Vite) ─┐        ┌─ CLI (Node.js, npx openvibe-cli) ─┐
│  资产管理 / 流程管理 / 标准包组装    │        │  sync 注入 / scan 扫描 / diff   │
└──────────────┬────────────────────┘        └───────┬────────────────────────┘
               │ REST/JSON                        读写│ (本地文件)
┌──────────────▼────────────────────┐        ┌───────▼────────────────────────┐
│  API Server (Node.js)             │ 标准包  │  目标项目目录                    │
│  资产/流程/标准包/版本/搜索服务      │◄──────►│  CLAUDE.md AGENTS.md .cursor/  │
└──────────────┬────────────────────┘  导出   │  TERMS.md skills/ CHECKLIST.md │
               │                            └────────────────────────────────┘
┌──────────────▼────────────────────┐
│  存储 SQLite(个人) / PostgreSQL(团队)│   分发目标 = 各 AI 编码平台的规则/skill 目录
│  FTS5 全文搜索 / 附件对象存储        │   （Claude Code、Cursor、CodeBuddy、Trae、MiniCode、zcode、Kimi Code、Codex…）
└───────────────────────────────────┘
```

### 4.2 关键设计决策

1. **标准包是纯文件契约**：Markdown/YAML，不发明私有格式。保证资产可迁移、可 git 版本化、工具锁定为零——生态多变环境下的生存策略。
2. **CLI 是 MVP 硬依赖**：浏览器无法写本地项目文件，「注入」必须由 CLI 完成；CLI 从 API 导出的标准包工作，也支持完全离线（本地文件模式）。
3. **平台适配层**：各目标平台的目录与文件格式（`.claude/skills`、`.cursor/rules/*.mdc`…）抽象为 adapter 表，平台契约变化只改 adapter。
4. **本地优先**：MVP 单用户数据全在本地（SQLite 文件），天然规避隐私问题；团队同步 Phase 2 再做。

### 4.3 文件结构（Monorepo，示意）

```
openvibe/
├── apps/
│   ├── web/          # SPA
│   ├── server/       # API Server
│   └── cli/          # npx openvibe-cli
├── packages/
│   ├── core/         # 领域模型 + 标准包生成器（web/cli 共用）
│   ├── adapters/     # 各平台目录契约 adapter
│   └── shared/       # 类型与工具
├── content/
│   └── seed/         # 预置术语/流程模板/精选提示词
└── docs/             # PRD、竞品调研、决策记录
```

### 4.4 外部依赖

- 无强制外部服务（MVP 可全离线跑；遥测默认关闭，关闭时零外联）
- P1：可选匿名遥测上报端点（**默认关闭**；公开事件仅三类：注入成功、流程模板选用、活跃项目去标识计数；不含任何路径/内容/用户标识；实现与端点选型见 design.md §11）；首次注入成功后一次性 opt-in 询问（D13，见 design.md §11.5 与 specs/onboarding.md FR-4.2）
- P1：GitHub API（skill 源拉取）、skills.sh 源、LLM API（测试场/草稿生成，用户自配 Key，AES-256-GCM 加密存储）

---

## 第 5 章 · 业务流分析

### 5.1 核心业务流：标准化飞轮 ⭐

```
 ①沉淀资产              ②组装标准包            ③注入项目
 (提示词/术语/技巧/      (M6: 挑选资产+流程模板   (CLI 写入 CLAUDE.md/
  skill 入库 M1-M4)  →   → 版本化 StandardPack)→  AGENTS.md/.cursor/TERMS.md)
        ▲                                           │
        │ ⑤回流新资产                                ▼
        └────────── (复盘产出: 新技巧/新词/改进提示词) ◄─ ④项目使用
                      M4/M3/M1 收录              (按流程开发 M5,
                                                  记 DEV/CHECK 日志)
```

闭环即护城环：只用一次是工具，转起来才是标准。

### 5.2 新项目启动流

```
创建项目(M5) → 选流程模板 → 选/组标准包(M6) → CLI 注入(dry-run 预览
→ 冲突则 diff+备份+确认) → 开工（项目文件里已有规则/术语/检查清单）
```

异常流：目标项目已有 `CLAUDE.md` → 展示三方 diff（现有文件 vs 标准包）→ 用户选择合并/覆盖/跳过 → 被覆盖文件自动备份到 `.openvibe/backup/`。

### 5.3 提示词生命周期流

```
创建 → 使用（复制/注入）→ 反馈标记（好用/翻车）→ 迭代版本（自动快照）
→ （团队版）提交评审 → 入选团队标准包
```

### 5.4 复盘回流流

```
项目复盘(M5 CHECK 日志) → 系统提取候选（新术语/新技巧/待改进提示词）
→ 人工确认编辑 → 入库(M1/M3/M4) → 下一个标准包自动更优
```

### 5.5 模块联动总表

| 触发 | 联动 |
|------|------|
| 标准包组装(M6) | 读取 M1–M4 素材 + M5 流程模板 |
| CLI 注入(M6) | 写目标项目目录；登记指纹供漂移检测 |
| 项目复盘(M5) | 生成 M4 技巧草稿、M3 术语候选、M1 提示词改进建议 |
| Skill 分发(M2, P1) | 经 adapter 写各平台目录 |

---

## 第 6 章 · 操作流分析

### 6.0 首次使用操作流（2026-09-20 澄清新增，D4）

```
npx openvibe-cli serve --open → 首启（播种 + 自动组装预置包）→ 向导 ≤3 步（可跳过）
→ 复制 sync 命令注入测试目录 → openvibe diff 体验漂移检测 → 进入日常操作流
```

### 6.1 日常操作流（开发者，每天 5-10 分钟）

1. 打开当日活跃项目的工作台 → 扫一眼看板与健康摘要
2. 勾选当前阶段检查清单；需要提示词时从库中搜出、填变量、复制
3. 下班前（或事件后）在开发日志里追加一条 DEV 记录（模板预填编号/时间/关联文件）

### 6.2 周期性操作流

- **每周**：资产整理 15 分钟（新提示词打标签归档、review 本周候选技巧）
- **每月**：跑一次 CHECK 报告（复用用户现行 40 轮检查机制，产品内置该模板）

### 6.3 事件驱动操作流

- **新项目**（10 分钟）：建项目 → 选模板 → 注入标准包 → 开工
- **项目复盘**（30-60 分钟）：过检查清单 → 写 CHECK 报告 → 确认回流候选

### 6.4 自动化操作流（系统侧）

- 提示词/skill 保存即版本快照（无需手动）
- 标准包导出时自动计算指纹并登记
- 注入时自动备份被覆盖文件
- P1：定时检测 skill 源更新（SHA-256 比对）并提醒

---

## 第 7 章 · 数据模型设计

### 7.1 核心实体（字段摘要）

```
Prompt          { id, title, content, variables[], tags[], platform_marks[],
                  status[draft|active|deprecated], owner_id, created_at, updated_at }
PromptVersion   { id, prompt_id, version_no, content, changelog, created_at }
Skill           { id, name, description, source[local|github|skillssh|manual],
                  latest_version, sha256, installed_targets[] }
SkillVersion    { id, skill_id, version, path, sha256, installed_at }
Term            { id, zh, en, aliases[], definition, example, related_term_ids[],
                  source, tags[], status }
TermProposal    { id, term_id?, proposed_term, proposed_change, status, reviewer_id }
Playbook        { id, title, scenario, problem, solution, anti_pattern,
                  link_prompt_ids[], link_skill_ids[], link_term_ids[],
                  score, verified_count }
Project         { id, name, local_path, flow_template_id, status, health,
                  standard_pack_id, standard_pack_version }
FlowTemplate    { id, name, kind[light|spec_driven|retro|custom],
                  stages[{ name, checklist[], artifacts[] }] }
Task            { id, project_id, stage_name, status, title, assignee_id, order }
DevLogEntry     { id, project_id, type[DEV|CHECK], entry_no, body,
                  evidence{ file, command, result }, created_at }
StandardPack    { id, name, version,
                  manifest{ prompt_ids[], skill_ids[], term_ids[], playbook_ids[],
                            flow_template_id }, artifacts_fingerprint }
User            { id, name, email, role }              // Phase 2
TeamMembership  { user_id, team_id, role[owner|member|reviewer] }  // Phase 2
```

### 7.2 实体关系

- Prompt 1—N PromptVersion；Skill 1—N SkillVersion
- StandardPack N—N (Prompt / Skill / Term / Playbook)，N—1 FlowTemplate（manifest 内引用，带版本快照）
- Project N—1 FlowTemplate，N—N StandardPack（按 version 锁定）
- Playbook N—N（Prompt / Skill / Term）——技巧可反向链接到资产
- Project 1—N Task、1—N DevLogEntry
- DevLogEntry.evidence 是复盘回流（5.4 节）到 Playbook 的数据来源

### 7.3 数据流转路径

```
资产录入 → DB(版本化) → manifest 组装 → 文件生成器 → StandardPack 导出
→ CLI 注入 → 项目目录(git 可追踪) → 项目使用/复盘 → 回流候选 → 资产层
```

---

## 第 8 章 · 开发优先级与里程碑

| Phase | 周期 | 交付物 | 验收标准 |
|-------|------|--------|----------|
| P0 设计定稿 | 1 周 | PRD 评审通过、架构与文件契约（标准包格式）定稿、adapter 清单 | ✅ **全部完成（2026-09-20）**：假设项澄清（第 0 章）；标准包契约冻结 v1（design §7）；adapter 清单冻结 v1.1 含国内平台（design §8）；遗留 A8 npm 名核验随 T1 |
| P1 MVP | 6.5 周（2026-09-20 裁定 D8：任务 31.5 人日 + 显式缓冲 1 人日） | Web（M1+M3 瘦身+M5+M6 组装）+ CLI（sync/scan/diff/备份）+ 种子内容（100 术语、3 流程模板、20 精选提示词）+ **开箱体验（预置包+首启向导含 lock 自动确认+飞轮面板＝本地统计卡片、不参与遥测，D19 消歧）** + 遥测薄模块（默认关）+ 三平台 CI + **六 adapter（含国内平台）** | 端到端走通标准化飞轮一圈：建库→组包→注入真实项目→复盘回流；新用户 ≤3 条命令/≤5 分钟完成首次注入 |
| **P1.1 退场与信任 + 发布运营**（2026-09-23 D19 开档） | 人日与退出定义**单源在 [tasks.md §2b](./tasks.md)**（本文不复制数字，防双源漂移） | **T10**：CLI `clean` 受管文件退场（只删未改动的，D20）+ 组包预览体量与 token 估算（中文感知）+ 一项目一包边界声明；**T11**：三条冒烟的真实输入证据（D21；其前置 S-2 探针**已于 2026-09-23 跑完**——有头三判据全过、无头判据①不可证，结论单源 `dev-plan §15.4-S2`；**三条冒烟场景本身已于 2026-09-24 接上骨架并跑通：有头 FAIL=0 SKIP=0，数字单源 `dev-plan §15.4b`**）+ `@import` 可行性探针结论 + 发布传播执行清单。**发布物版本号 v0.2.0**（阶段名 ≠ 版本号；全是向后兼容的新增，minor 级） | 规格过 owner 复核 → 实现 + §2b 验收行列出的测试族**三平台 CI 全绿**（具名族与支数**单源在 [tasks.md §2b](./tasks.md) 验收行**，本文不复制，防双源漂移——本列曾抄过 `CLI-CLEAN-01..10`/`CORE-SIZE-01..05`/`SRV-EST-01..03` 一组，既与左栏自述相矛盾、又被实落否证：实落支数与该枚举两处不等）+ **带 ref 锚的** A 级冻结契约零改动凭据（`git diff --name-only <分叉点>..HEAD -- tests/golden/ content/seed/` → 0 行；**不带 ref 的形态按 dev-plan §13 DoD 第 7 条只作工作树补充，不是机器凭据**）。细则见 [dev-plan.md §15](./dev-plan.md)。⚠️ 本列是**退出定义**，不是已达状态 |
| P2 生态与团队 | 3 周 | M2 完整（源导入+多平台分发）、M4 技巧库（D22 确认 Phase 2 归属）、M7 多用户与评审；**D19 自 P1.1 移入**：遥测增长看板（对外公开页，非 MVP 已交付的本地飞轮卡片）、暗色主题（前置是设计 token 化重构）、`@import` 受管子文件落地（A 级契约变更，需 `schemaVersion +1`）、一项目多包叠加 | skill 一键分发 ≥5 平台；技巧回流闭环可用 |
| P3 智能与打磨 | 3 周 | AI 测试场、复盘草稿自动生成、双向同步、桌面端评估 | AI 功能用户自配 Key 可用 |

**关键依赖**：CLI 必须与 Web 同步在 P1 交付（注入唯一通道）；种子内容（术语/模板）与开发并行准备；三平台 CI 自 T1 生效（D5）。

**P1.1 的前置关系（D19，2026-09-23）**：P1.1 的规格与 T10 代码实现**不以 v0.1.0 已发布为条件**；但 T11 的冒烟证据归档与传播清单**以真发布为执行条件**（发布四步——真 `npm publish --registry=https://registry.npmjs.org` / `gh release create` / 推 tag / 种子人工审校——仍在 owner 手上，不在 P1.1 范围内）。人日与退出定义单源在 [tasks.md §2b](./tasks.md) 与 [dev-plan.md §15](./dev-plan.md)，此处不重复。

**工时量级（2026-09-20 裁定 D8 定案；D14 修订内容产能）**：单人全栈任务 ≈ **31.5 人日 + 显式缓冲 1 人日 = 6.5 周窗口**；内容产能（D14，2026-09-20 评审修订）：原「0.5 名内容编辑 ≈6.5 人日并行轨」取消，改为 **AI 起草初稿 + owner 审校 ≈ 2-3 人日**（含于窗口）。构成 = 原澄清增量（遥测/预置包/向导/三平台 CI）+ 国内平台扩充（codebuddy/trae/minicode 三 adapter）+ 向导 lock 自动确认（D11）；P0 期间已完成平台契约核验与 npm 名核验（A8：`openvibe` 可用），T1 不再含核验工作。偏差超出缓冲时启用 tasks §3 变体备案。

---

## 第 9 章 · 风险与缓解

| 风险 | 类型 | 概率 | 影响 | 缓解 |
|------|------|:---:|:---:|------|
| 各平台目录契约频繁变化（Cursor/Claude 规则格式演进） | 技术 | 高 | 高 | adapter 层隔离 + 社区共建映射表；标准包纯 Markdown 底线不失效 |
| 用户没有意愿维护术语库（冷启动死） | 产品 | 高 | 高 | 预置 100+ 词条开箱即用；复盘流自动产出候选，人工只做确认 |
| 与 PromptHub 功能撞车，被评价「又一个资产管理器」 | 产品 | 中 | 中 | 定位差异化：流程+知识+团队闭环；不追求资产管家全功能 |
| CLI 写用户项目文件的信任问题 | 技术 | 中 | 高 | dry-run 默认、自动备份、显式确认三重保护 |
| 生态变化快导致产品形态过时（如平台原生支持资产同步） | 运营 | 高 | 中 | 数据格式纯 Markdown 可迁移，零锁定；价值锚在「流程与知识」而非文件搬运 |
| 社区冷启动（开源无人用） | 运营 | 中 | 中 | 先自用自证（dogfooding）；内容先行（术语库/技巧库单独就有检索价值） |
| 提示词量过万后搜索与组织退化 | 技术 | 低 | 中 | SQLite FTS5 起步；量级上来后引入标签体系治理 |
| 遥测引发社区信任疑虑（对「写文件的 CLI + 上报」双重敏感） | 产品 | 中 | 高 | 默认关闭 + 事件清单三类白名单公开 + 代码可见 + 设置一键关；README 显著披露（D3，design.md §11） |
| Windows 兼容成为支持负担（社区受众跨平台） | 技术 | 中 | 中 | CI 三平台矩阵自 T1 生效（D5）；路径/权限/换行符跨平台纪律；MVP 不引入 symlink 依赖（无 skill 分发） |

---

## 附录 A · 竞品调研摘要

立项快照见 [competitive-research.md](./competitive-research.md)（2026-09-20 上午）；**深度对比见 [competitive-analysis.md](./competitive-analysis.md)（同日）**，其关键更新：① 出现「规则同步」新品类（rulesync/Rulix/rulebook-ai 等，与 M6 概念空间重叠）——验证需求但要求差异化锚定「组装+治理+回流」；② AGENTS.md 已成 Linux 基金会标准（60k+ 项目），兼容矩阵策略受益；③ spec-kit ~90k★ 但维护存疑，流程模板可视化裁剪的裂缝扩大；④ 国内平台规则契约覆盖为全场独占（第一张传播牌）。核心结论不变：术语库、技巧库、团队标准包闭环三块仍为空白，OpenVibe 错位竞争。

## 附录 B · 术语库种子词条示例（节选）

| 中文名 | 英文名 | 定义摘要 |
|--------|--------|----------|
| 氛围编程 | vibe coding | 以自然语言意图驱动、AI 生成代码、人负责意图与验收的开发方式 |
| 提示词 | prompt | 喂给模型的任务指令文本，含上下文、约束与期望输出 |
| 技能 | skill | 打包为 SKILL.md 的可复用能力单元，供编码代理按需加载 |
| 规则文件 | rules / CLAUDE.md / AGENTS.md | 注入项目的持久化行为约束，决定 AI 的默认工作方式 |
| 上下文窗口 | context window | 模型单次可感知的输入长度上限，超出即截断或压缩 |
| 规则漂移 | rule drift | 长会话中模型逐渐偏离规则文件约束的现象 |
| 规格驱动开发 | spec-driven development | 先写规格（spec）再生成代码的开发范式（spec-kit/BMAD 等） |
| 幻觉 | hallucination | 模型生成看似合理实则错误的内容 |
| 检查点 | checkpoint / freeze | 冻结当前状态以便安全回退的机制 |
| 检索增强生成 | RAG | 检索外部知识再生成，缓解幻觉与知识过时 |

## 附录 C · 与现行开发规范的兼容

M5 开发日志模板内置对「DEV-NNNN 编号递增 / YYYY-MM-DD HH:mm 时间戳 / 关联文件 / 测试验证 / 40 轮 CHECK 检查」规范的兼容——OpenVibe 自身即按该规范开发（本 PRD 的产出已记录为 DEV-0001），作为第一个 dogfooding 案例。

## 附录 D · 三维审查记录与变更日志

**审查执行**: 2026-09-20，按 prd-iterative 技能 Phase 3 执行，完整审查表见交付报告，摘要如下：

### 审查一 · 逻辑一致性
- ❌ **[P0] 术语注入通道断裂**：初稿 M3 直写「注入上下文」，但 Web 架构无法写本地文件 → 修正为 M3 仅产出 TERMS.md 素材，注入统一走 M6+CLI（已修入 3.2/4.2 节）
- ❌ **[P0] 复盘回流数据链缺字段**：DevLogEntry 缺 evidence 结构 → 已补（7.1 节）
- ✅ 数据流闭环、操作流覆盖、优先级与 Phase 一致性（修正后）通过

### 审查二 · 批判性
- ❌ **[P0] Web-only 架构物理不可行**：本地项目注入在浏览器沙箱内无法实现 → CLI 升级为 MVP 硬依赖（已修入 4.2/8 章）
- ⚠️ [P1] 术语库维护意愿假设不现实 → 冷启动预置 + 自动候选（已修入第 9 章风险表）
- ⚠️ [P1] AI 测试场属过度工程前置 → 降级至 Phase 3
- ⚠️ [P2] 数据量 10 倍增长的搜索退化 → FTS5 + 标签治理备忘

### 审查三 · 第一性原理
- 核心价值一句话：**把 vibe coding 从手艺变成工程——资产可复用、流程可复制、知识可传承**
- 只保留 3 个功能：M1 提示词库、M5 流程管理、M6 标准包注入 → MVP 据此收敛；M3 以「预置内容瘦身版」保留（差异化但降投入），M2 瘦身，M4/M7 后移

### 澄清决策记录（2026-09-20，brainstorming 对话定稿）

| # | 决策 | 落点 |
|---|------|------|
| D1 | 中文社区开源优先 | 第 0/1/2 章 |
| D2 | 双钩子：标准包注入 + 流程模板（G1+G2 主、G3 支撑） | 1.3、3.2-M3 |
| D3 | 可选匿名遥测：默认关、事件清单公开、可随时关 | 1.4、4.4、第 9 章 |
| D4 | 预置演示包 + 可跳过 ≤3 步首启向导 + 飞轮面板 | 3.4、6.0 |
| D5 | macOS/Linux/Windows 三平台硬要求 | 第 8/9 章 |
| D6 | 协议 Apache-2.0（2026-09-20 二次裁定正式定案） | 第 0 章第 10 行 |
| D7 | adapter 扩充国内主流平台：zcode/Kimi Code 走兼容矩阵（AGENTS.md） | design §8、3.2-M6、第 8 章 |
| D8 | 工期改承诺 6.5 周：任务 31.5 人日 + 显式缓冲 1 人日（修订原 G6 六周口径） | 第 8 章 |
| D9 | MiniCode 经 README 实证升为专用 adapter（`MINI.md`） | design §8、3.2-M6 |
| D10 | Trae adapter 改用 `.trae/rules/openvibe.md` + frontmatter `trigger: always`（契约 v1.2 微修订，社区+半官方来源确认多文件支持） | design §8 |
| D11 | 首启向导「我已注入」改为 lock 文件自动检测（手动按钮降级保留） | specs/onboarding.md FR-2.4、6.0 |
| D12 | 遥测端点定案：自写极简计数端点（单文件 Cloudflare Worker，owner 自部署） | design §11.5、4.4 |
| — | A8 关闭：npm 包名 `vibecanon`（**旧名**）核验可用（registry 404，2026-09-20 实测）——**D17 更名后作废**，由 `openvibe-cli` 承接（registry 404，2026-09-21 实测） | 第 0 章第 14 行、tasks G5、附录 D-D17 |
| D13 | 遥测「惊喜时刻」询问：首次 sync 成功后一次性 opt-in（CLI+Web 双入口；默认关、拒绝后本地标记永不再问；白名单三类不变） | design §11.5 + D18、specs/onboarding.md FR-4.2、4.4、tasks T7/T8 |
| D14 | 内容产能：owner 本人 + AI 起草 + 审校 ≈ 2-3 人日；60 条词条首发可接受（seed 热补至 100）；6.5 周维持 | 第 8 章、1.4 内容层、tasks T4/T8/§3、seed-content 执行角色 |
| D15 | M5 dogfooding 证伪线：发布后两周内工作台真实使用 ≥10 次且 DEV 日志 ≥5 条，否则启动 M5-lite（工作台 UI 后移 P1.1，模板/清单仅经 CHECKLIST.md 注入） | tasks §5 风险表 |
| D16 | 仓库策略：新开 GitHub `OpenVibe` 仓库（初始提交 = 本套文档 + LICENSE + .gitignore），不携带现仓库 Hotspot（GPL-3.0）历史；旧仓库仅存档 | tasks T1、README 当前状态 |
| D17 | 项目更名 VibeCanon → **OpenVibe**（中文名沿用灵典）：npm `openvibe` 被占（registry 200）→ 包名与冷启动命令 `openvibe-cli`（404 实测），bin `openvibe`；GitHub 同名小仓库与 Inria OpenViBE 撞车已披露，传播以「灵典」区隔；契约字符串随 design §7/§8 **v1.3** 更新（文件名 openvibe.pack.json / .openvibe/ / marker openvibe:pack，结构不变） | 第 0 章第 14 行、README 名称由来、design §7/§8、全文档机械替换（DEV-0010 映射表） |

| D19 | **P1.1 范围裁定**（owner 选「A+C 打包，其余归 P2」，2026-09-23）：入选 = **A 批 退场与信任**（`clean` / 预览体量与 token 估算 / 一项目一包边界声明，任务组 T10）+ **C 批 发布运营与证据**（三条冒烟真实输入 / `@import` 探针 / 发布传播清单，任务组 T11）；**归 P2** = 暗色主题（前置是设计 token 化重构，实测全仓 `dark`/`prefers-color-scheme` 零命中）、遥测增长看板、M4 技巧库（见 D22）、`@import` 落地（A 级契约变更）、一项目多包叠加；**判作废** = 「首启向导后移 P1.1」「国内三 adapter 后移 P1.1」（两者 MVP 已交付，属文档陈旧）；**条件挂起** = M5-lite（D15 观察窗从**真发布**起算，而 v0.1.0 未发布：npm 404 / `gh release list` 空 / tag 仅本地，现在无从判定）。版本号口径：**阶段名 P1.1 ≠ 发布物版本**，发布物走 **v0.2.0**（新增命令与新增响应字段都是向后兼容的 minor） | 第 8 章 P1.1 行、dev-plan §15（裁定表 13 项 + DoD 5 条）、tasks §2b（T10/T11） |
| D20 | **`clean` 的删除边界 = 只删未改动的受管文件**：`IN_SYNC`（lock 登记且磁盘与注入时逐字节一致）→ 备份后删除；`DRIFT`（用户注入后改过）→ **默认保留不删**，仅显式 `--force` 才删；三态之外一律 `FOREIGN`，**任何情况下不删**。理由：退场是破坏性动作，而用户手改过的文件是唯一无法从包里恢复的东西。（分类细节、lock 去留、路径净化与退出码以 spec 为准，本文不复制） | `specs/m6-cli-injection.md` FR-6 + §6.10 + §7.10 验收 a–j（含可重放律 `sync → clean → sync → diff = 0`） |
| D21 | **三条冒烟的证据口径：不引入 Playwright，改扩现有裸 CDP 驱动器加真实输入**（`Input.dispatchMouseEvent` / `dispatchKeyEvent` / `insertText` + clipboard 权限）。前置 **S-2 探针**三判据（各自独立成立才算过）：① `clipboard.writeText` 下系统剪贴板真拿到内容（只看 promise resolve 不算过）；② 中文串经 `insertText` 进受控 input 后 debounce 检索命中；③ Radix Dialog 内 Tab/Enter 能驱动焦点转移与确认。**未跑之前不得当成已通过**——**该探针已于 2026-09-23 跑完：有头 Chrome 三条全过；无头下①结构上不可证**（`writeText` resolve、页内 `readText` 读得到、toast 出现，而系统 pasteboard 一字未动），故判据①只认有头证据，无头 CI 不得声称已证。退路：不过则三条冒烟永久记为人工录屏缺口，并写进发布说明「本版未验证」段——**该退路未触发**：三条冒烟场景已于 **2026-09-24** 各自接上该骨架并跑通（**有头 FAIL=0 SKIP=0**；无头 FAIL=0，两条系统剪贴板腿按设计记 SKIP 而非 PASS），驱动器 `docs/devlog-evidence/DEV-0025/three-smokes.mjs`，v0.1.0 发布说明的三条未验证项据此勾销。数字与限制的单源在 `dev-plan §15.4b`，本文只写状态 | dev-plan §15.4-S2 / §15.4b、tasks §2b·T11 与 §4·T9 冻结注、design §2 测试行 / §13 E2E 行、proposal 第 5 章交付物 4 |
| D22 | **M4 技巧库确认归 Phase 2 + PRD 勘误**：3.2-M4 标题原标「（P1）」，与本文 3.3「M2 完整版、M4、M7 进 Phase 2」及第 8 章 P2 行冲突，且 `docs/specs/` 无 m4 规格、tasks/dev-plan 全文零次提及 M4——既未实现也未被显式裁掉。裁定以 Phase 归属为准，写法沿用 M7 的「（P1，Phase 2）」惯例；进入实施前须先补 spec（dev-plan §0.4 No-spec-no-task） | 3.2-M4（本次勘误留痕）、第 8 章 P2 行、dev-plan §15.1-9 / §15.5-5 |

### 变更日志
| 版本 | 日期 | 变更 |
|------|------|------|
| v0.1 | 2026-09-20 | 初稿（9 章全量） |
| v0.1.1 | 2026-09-20 | 三维审查 P0×3 修正：CLI 硬依赖、术语注入通道、DevLog 字段；MVP 边界收敛 |
| v0.1.2 | 2026-09-20 | owner 两轮沟通定稿（原 v0.2.0+v0.2.1 合并重编号）：D1–D7（社区优先 / 双钩子权重 / 可选遥测 / 开箱体验 / 三平台硬要求 / Apache-2.0 / 国内平台扩充）；成功指标双层口径；标准包契约冻结 v1（G2）、adapter 清单冻结 v1.1（G3）；P1 工期 6 周/32 人日口径，保 4 周变体备案 |
| v0.1.3 | 2026-09-20 | P0 收尾与评审修订（原 v0.2.2+v0.2.3 合并重编号）：D8–D12（工期 6.5 周 = 31.5+1 人日 / 契约 v1.2 / A8 包名核验关闭 / 向导 lock 自动确认 / 遥测端点自写极简）；D13–D16（遥测首次注入一次性 opt-in 询问 / 内容产能 AI 起草+owner 审校、60 条首发可热补 / M5 两周证伪线 / 新仓库策略）；一致性修复（tasks T7/T9 人日、m5 端点名、竞品报告裸域链接） |
| v0.1.4 | 2026-09-21 | **D17 项目更名 OpenVibe**（原 VibeCanon，中文名沿用灵典）：npm 包名与冷启动命令 `openvibe-cli`（openvibe 被占，实测）；契约字符串升 design §7/§8 v1.3（结构不变）；全文档机械替换 + 历史核验语句保留旧名事实（DEV-0010 映射表） |
| v0.1.5 | 2026-09-23 | **P1.1 开档（D19–D22）**：第 8 章新增 P1.1 行（T10 退场与信任 + T11 发布运营与证据，≈4 人日，**发布物版本号 v0.2.0**——阶段名 ≠ 版本号）与「P1.1 前置关系」段；3.2-M6 补两行功能（`openvibe clean` 退场 / 预览体量与 token 估算）；3.2-M4 勘误改判 Phase 2（D22，消除 3.2 标题 vs 3.3 vs 第 8 章的三处冲突）；**「飞轮面板」同名异物消歧**（MVP 已交付的本地统计卡片 ≠ P2 的遥测增长看板，D19）；`clean` 删除边界定案 = 只删未改动的受管文件（D20）；三条冒烟证据口径 = 裸 CDP 真输入、不引入 Playwright（D21） |
| v0.1.6 | 2026-09-23 | **S-2 探针结论回填（只改状态，不改任何裁定）**：D21 行的「未跑之前不得当成已通过」补上实况——有头 Chrome 三判据全过、无头下判据①（系统剪贴板）结构上不可证；第 8 章 P1.1 行标注 T11 前置解除。三条冒烟场景**仍未跑**，v0.1.0「本版未验证」清单不变。数字与限制的单源在 `dev-plan §15.4-S2`，本文只写状态 |
| v0.1.7 | 2026-09-24 | **三条冒烟收口回填（只改状态，不改任何裁定）**：D21 行的「三条冒烟场景本身仍未跑」改为实况——三条场景已各自接上 DEV-0024 的裸 CDP 真输入骨架并跑通（驱动器 `docs/devlog-evidence/DEV-0025/three-smokes.mjs`，**有头 FAIL=0 SKIP=0 / 无头 FAIL=0 SKIP=2**，两条 SKIP 都是系统剪贴板腿）；人工录屏退路未触发；第 8 章 P1.1 行的 T11 同步标注。**v0.1.0 发布说明的三条未验证项据此勾销**，勾销依据是构建同源判定（`3e852c2..HEAD` 无任何非文档改动），不是「测了个更新的构建」。数字与三条方法级副产品的单源在 `dev-plan §15.4b`，本文只写状态 |
