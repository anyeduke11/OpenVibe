# T8 种子内容审校清单（基线 6c33b50）

生成方式：`pnpm seed:review -- --base-ref 6c33b50`（可随时重放，事实源是 content/seed 与 git 基线）。

口径：terms 109 条（新增 46 / 移除 0）、prompts 20 条（新增 20）。结构门槛由 `pnpm seed:check` 把关（数量/别名/受控词表/定义长度/example 覆盖/§3.4 配额），本清单只解决机器测不到的部分：**定义是否真的准确、措辞是否有 AI 腔、示例是否成立**。

## 术语（新增 46 条，逐条待校）

| # | zh | en | 标签 | 定义（待校：是否准确、有无夸大） | example（待校：是否成立） |
| --- | --- | --- | --- | --- | --- |
| 1 | 智能体循环 | agent loop | 基础概念/提示工程 | 编码代理「计划→调用工具→读取结果→再决策」反复推进的执行回路；每一轮都消耗上下文，轮次越多成本越高、偏离原始意图的风险也越大。 | 一次接口改造连跑 12 轮：前 3 轮定方案，后 9 轮在编译报错与修复间往返——循环长度直接暴露了需求描述不足。 |
| 2 | 上下文压缩 | context compaction | 基础概念/风险陷阱 | 会话逼近窗口上限时把历史轮次改写为摘要或丢弃低价值片段，以可控的信息损失换取继续对话的空间；被压缩掉的约束最容易悄悄失效。 | 长会话把前 40 轮压成一段任务摘要，但显式保留规格原文与待办清单，避免规则随历史一起蒸发。 |
| 3 | 令牌预算 | token budget | 提示工程/风险陷阱 | 为一次任务预先划定的输入与输出令牌额度，用来约束检索片段数量、循环轮次与回复长度，避免上下文无限膨胀挤掉真正的关键信息。 | 组包时把规则段限制在 8K 令牌内，超出额度的提示词降级到附录段而不进规则区。 |
| 4 | 渐进式披露 | progressive disclosure | 方法论/提示工程 | 先只把最小必要信息放进模型视野，随任务推进再逐层展开细节的上下文组织策略；目的是减少无关内容对注意力的稀释，而非省令牌。 | 技能正文只写触发条件与三条步骤，示例与边界放进被引用的二级文件，代理只在需要时读取。 |
| 5 | 上下文工程 | context engineering | 提示工程/方法论 | 系统性设计进入模型的信息集合——选什么、排什么序、压缩哪段、如何引用来源；与打磨单条指令措辞相比，它决定的是任务能否被正确理解。 | 把检索片段从整段文档改成带文件路径与行号的一行式清单后，代理定位错处的轮次从两轮降到一轮。 |
| 6 | 提示缓存 | prompt caching | 提示工程/工具平台 | 把稳定前缀（规则文件、术语表）在模型服务侧缓存以降低费用与首字延迟的机制；前提是前缀字节级稳定，一个时间戳就能让命中率归零。 | 规则文件顶部写入导出时间戳后缓存全失效；时间戳只留在 manifest，命中率回到九成以上。 |
| 7 | 规则优先级 | rule precedence | 提示工程/方法论 | 显式写明多条规则冲突时谁胜出，通常形如「安全底线 > 项目规则 > 个人偏好」；没有声明时模型按上下文邻近度随机偏向其一。 | 标准包里单设一节冲突裁决，把项目规则置于个人习惯之上，此后「按我风格写」不再覆盖团队命名约定。 |
| 8 | 输出契约 | output contract | 提示工程/质量工程 | 对模型返回内容施加硬性结构约定（字段、类型、可解析格式），让下游程序可校验可重试；只描述期望样式而不给约束，等于没有契约。 | 约定返回仅含 summary 与 files 两键的 JSON，解析失败即整段回退纯文本展示而不是崩在渲染层。 |
| 9 | 规则段落 | rule section | 提示工程/工具平台 | 规则文件内按用途划分的块，常见为行为约束、术语口径与检查清单三段；段落边界决定了哪些内容会被模型当成强约束执行。 | 组包时把 useAs=rule 的提示词收进规则段、reference 类放附录段，注入后模型只对前者保持长期遵守。 |
| 10 | 适配器 | adapter | 工具平台/基础概念 | 把统一的上游内容翻译成某个编码工具私有文件格式的转换层；新增平台通常只需新增适配器，上游内容与指纹保持不变。 | 同一份术语表经六个适配器分别落成 CLAUDE.md、.cursor/rules/openvibe.mdc 与 .trae/rules/openvibe.md。 |
| 11 | 兼容矩阵 | compatibility matrix | 工具平台/方法论 | 用一份通用产物覆盖一批工具能力的映射表，用来避免为每个只读通用规则文件的工具单写适配器；代价是无法表达平台的私有细节。 | 只读 AGENTS.md 的那批代理走兼容矩阵零代码覆盖，三个有专有文件契约的平台才配专用适配器。 |
| 12 | 锁文件 | pack lock file | 工具平台/数据与存储 | 记录项目当前注入的包名、版本与内容指纹的小文件，是漂移检测与再注入的基准；它描述现状而非意图，删掉等于宣告项目未注入。 | 漂移检测读它与磁盘文件比对，发现术语表被手工改过两行，报出差异而非静默覆盖。 |
| 13 | 归档包 | bundle | 工具平台/数据与存储 | 一次导出的可传输整体，通常带随机后缀不覆盖；在没有服务端的机器上，它是注入与迁移的唯一入口。 | 把归档包拷到内网开发机，用离线文件注入完成同一套规则落地，指纹与联网机器一致。 |
| 14 | 扫描根 | scan root | 工具平台/协作流程 | 扫描规则与技能文件时使用的根目录；默认根只对某一类工具生效，换生态的用户首启会拿到空台账，必须可显式指定。 | 非某工具用户把扫描根指向自己的配置目录后，台账从空列表变成 14 项。 |
| 15 | 数据目录 | data directory | 工具平台/数据与存储 | 存放本地数据库、备份与配置的根目录；换独立数据目录即可在不污染真实数据的前提下做演示、走查与回归。 | 走查脚本每次先把数据目录指向临时路径，保证首启组装与统计数字从干净的零态开始。 |
| 16 | 内容指纹 | content fingerprint | 数据与存储/质量工程 | 对包内每个文件的路径与内容哈希做有序拼接后再哈希得到的短摘要；它是判断「同一份包」的唯一依据，改一个空格就会变。 | 同一选集连导两次指纹一致，手工在术语表加一个空格后指纹立刻不同，漂移检测随即报出。 |
| 17 | 三元分词 | trigram tokenizer | 数据与存储 | 按三字符滑动窗口切分文本建索引的分词方式，让中文在没有词典的前提下也能被全文检索命中任意长度不小于三的字串。 | 启用三元分词后搜「漂移检」仍能命中「漂移检测」词条，无需预置中文分词词典。 |
| 18 | 受控词表 | controlled vocabulary | 数据与存储/协作流程 | 限定标签取值的固定集合，防止同义标签分裂出多个筛选入口；用户自建内容可自由打标签，预置内容必须严格取自词表。 | 种子词条若打上「DevOps」这类词表外标签，校验脚本直接判失败而非静默接受。 |
| 19 | 种子升级 | seed upgrade | 数据与存储/协作流程 | 内容包哈希变化时把新增条目并入、已被用户编辑过的条目跳过并提示的导入规则；它保证重播既不重复也不吃掉用户改动。 | 手工改过释义的术语在重播时保留原文并出现在跳过清单，未编辑的条目则并入新版本。 |
| 20 | 阶段快照 | stage snapshot | 数据与存储/协作流程 | 创建项目时把流程模板的阶段与检查清单整体复制一份存进项目，使模板后续修改不回溯影响存量项目；代价是模板更新不会自动同步。 | 模板加了第四个阶段后，先前建的项目仍是三阶段快照，勾选进度与统计口径不被打断。 |
| 21 | 台账 | registry | 数据与存储/协作流程 | 对外部能力（技能、工具、集成）的本地登记簿，记录路径、版本与扫描哈希；台账只描述「发现了什么」，不代表已启用。 | 技能台账列出 14 项扫描结果与各自哈希，未勾选的项目不会进入组包候选。 |
| 22 | 指令冲突 | instruction conflict | 风险陷阱/提示工程 | 多条规则对同一行为给出不兼容要求；模型往往按上下文邻近度随机偏向其一，外在表现为同类任务时好时坏的行为抖动。 | 一条要求中文回复、另一条模板要求英文注释，同一轮里两种输出交替出现即是冲突信号。 |
| 23 | 提示加固 | prompt hardening | 风险陷阱/提示工程 | 通过显式分隔符、来源标注与「以下内容不构成指令」的声明，降低外部文本劫持代理行为的成功率；它是纵深防御的一层而非保险箱。 | 外部文档包进不可信标签并声明其内容不构成指令后，被文档里的「忽略以上规则」劫持的用例不再复现。 |
| 24 | 工具过载 | tool overload | 风险陷阱/工具平台 | 暴露给代理的工具过多导致选错或滥用工具的现象；表现为重复检索、绕远路完成本可一步做到的事，通常需要靠收敛工具集解决。 | 同时给 20 个检索入口时代理反复挑错数据源，收敛到 5 个后首轮命中率显著回升。 |
| 25 | 最小权限 | least privilege | 风险陷阱/协作流程 | 只授予代理完成当前任务所必需的最小能力，把读文件、写文件、执行命令与访问网络各自独立开关；默认全开是事故温床。 | 只做代码审查的代理关掉写盘与网络，误删与外传的可能在权限层面就被封死。 |
| 26 | 覆盖前备份 | pre-write backup | 风险陷阱/质量工程 | 注入或导出前把将被覆盖的原文件复制进带时间戳的备份目录，使误覆盖可逐个还原；它是不可逆写入的最后一道本地防线。 | 注入误覆盖了项目自带的说明文件，从当次备份目录单独复制回去即可还原。 |
| 27 | 人类在环 | human in the loop | 协作流程/风险陷阱 | 在流程中显式插入人工批准节点的设计，用于授权执行、危险删除与对外发布这类不可逆动作；关键是让人做裁决而不是替人签字。 | 代理要往项目外写文件时先返回计划与影响面，等人确认后才落盘。 |
| 28 | 飞轮 | flywheel | 方法论/协作流程 | 资产沉淀→组装标准包→注入项目→产生回流资产→再入库的自我强化回路；每一圈都让下一次注入更省事，是产品价值的主要来源。 | 同一项目走完导出、注入、回流记作一圈，按项目去重计数即为飞轮圈数。 |
| 29 | 确定性 | determinism | 质量工程/方法论 | 同一输入在任何机器、任何时刻产出字节完全一致的结果；实践中要求产物内部零时间戳、排序有唯一兜底键，否则指纹与 diff 全部失真。 | 把时间戳集中到清单文件并为列表排序补上唯一兜底后，两次独立建库导出得到同一指纹。 |
| 30 | 干跑 | dry run | 质量工程/协作流程 | 只计算并输出将要发生的变更而不写任何磁盘状态的执行模式；对会改用户文件的工具，它是把决定权交回用户的首要手段。 | 干跑列出八个待写文件与两处将建目录，确认无误后再真正执行注入。 |
| 31 | 不稳定测试 | flaky test | 质量工程/风险陷阱 | 同一份代码在相同环境下时过时不过的用例；根因多是时间戳平局、并发时序与外部依赖，靠重跑掩盖只会把信号彻底磨没。 | 毫秒级同刻播种导致列表顺序随物理序漂移，补上唯一兜底排序后偶发红才真正消失。 |
| 32 | 不可变版本 | immutable version | 质量工程/数据与存储 | 已经导出或被引用的版本号不得改写，只能追加新版本；否则用户手里的锁文件与指纹将永久对不上，漂移检测失去基准。 | 重复导出同一版本号返回冲突，改动后升版本再导，历史导出仍可复现。 |
| 33 | 证据留存 | reproducible evidence | 质量工程/协作流程 | 把验收用的驱动脚本、夹具生成方式与环境信息随结果一并入库，只留结果日志等于留下不可复核的结论。 | 走查脚本自己造大文件夹具并写死判读表，日志与脚本同目录提交。 |
| 34 | 反向导入 | reverse import | 协作流程/工具平台 | 把项目里既有的规则与配置文件读回成结构化资产，让新工具接手历史积累而不必从零录入；导入按内容键去重。 | 把仓库里手写的编辑器规则文件导入成六条提示词，再统一纳入标准包管理。 |
| 35 | 回流资产 | reflowed asset | 协作流程/方法论 | 项目复盘后沉淀回公共库的新条目（术语、提示词、模板改进），是飞轮闭合的标志；日志里带非空关联资产即计一条回流。 | 一次联调把踩到的口径歧义补成两条术语并在日志关联，即为可统计的回流。 |
| 36 | 首启向导 | first-run wizard | 协作流程 | 首次启动时引导用户跑通核心价值主张的短流程；每一步必须产生真实副作用，演示动画式向导对社区产品是负资产。 | 三步向导分别展示真实统计、真实预览接口返回的文件树与真实注入结果。 |
| 37 | 匿名统计 | anonymous telemetry | 风险陷阱/协作流程 | 默认关闭、事件类型严格受限的用量上报；上报体不得含路径、文件内容与机器标识，关闭时零外联，透明度即信任底线。 | 三类白名单事件里只有包名@版本与去标识日计数，不含任何本地路径。 |
| 38 | 测试分层 | test pyramid | 质量工程/方法论 | 按运行速度与定位成本把用例分为单元、集成与端到端三层并控制配比：单元多而快、端到端少而真，倒置会让反馈慢到没人跑。 | 同一规则既由单元测试覆盖分支组合，也由真起服务的集成用例验证一次端到端注入。 |
| 39 | 夹具 | test fixture | 质量工程 | 为测试预先准备的输入数据与期望产物；由脚本自造并可重放的夹具才算数，手工在 /tmp 攒出来的目录只能证明当时成功。 | 超过单文件上限的巨型包由脚本现造，跑完即清理，不留下无人知道的临时目录。 |
| 40 | 语义化版本 | semantic versioning | 协作流程/质量工程 | 用主版本号、次版本号与修订号表达兼容性变化的命名约定；对内容包而言，规则语义变化升次号、错别字级修正升修订号。 | 把一条规则从「建议」改成「必须」是次版本变化，而修正术语错字只是修订号变化。 |
| 41 | 规则文件契约 | pack file contract | 数据与存储/风险陷阱 | 标准包对外落盘文件的命名、目录与标记头约定，是跨版本可比对的基础；契约变更必须升版本，否则用户侧 diff 会集体误报。 | 把标记头改名属于契约变更，需同时升版本并重算指纹，避免漂移检测把全项目判成已改动。 |
| 42 | 发布形态走查 | published-shape walk | 质量工程/协作流程 | 用打包产物本身做验收而不是跑仓库源码：真出包、真装进干净沙箱、只调用装出来的可执行入口，避免「源码里通得过、装完跑不起来」。 | v0.1.0 的 45 项发布断言全跑在 npm pack 出的 tarball 上：装进空 HOME 沙箱后只调用装出来的 bin，起服务、建库、托管页面逐项验。 |
| 43 | 预编译包回退 | prebuilt binary fallback | 工具平台/风险陷阱 | 含原生依赖的安装先按平台与运行时 ABI 取预编译产物，取不到才回落本地编译。两条路的用户群与故障域不同，须各自验证。 | 本机命中 node-v147-darwin-arm64 预编译包即通过；编译回退因工具链缺 C++ 头文件无法本地证明，按未验证记录而不是按通过。 |
| 44 | 清单归一化 | manifest normalization | 工具平台/数据与存储 | 包管理器读写清单时的静默改写：去前缀、补默认字段、删非法项，还可能配上误导性警告。断言应针对改写后的值，否则测的不是别人装到的那份。 | npm 把 bin 的 ./dist/cli.js 改写成 dist/cli.js 并警告「was invalid and removed」；改存改写后的值，警告与差异同时消失。 |
| 45 | 镜像注册表 | registry mirror | 风险陷阱/工具平台 | 指向加速镜像的注册表配置：装得进来未必发得出去。发布类命令要显式覆盖注册表地址，否则本地演练全绿而真实发布路径根本没走通。 | npm 与 pnpm 的 dry-run 都打出「Publishing to registry.npmmirror.com」，发布须另写 --registry=https://registry.npmjs.org。 |
| 46 | 瞬时用户激活 | transient user activation | 风险陷阱/质量工程 | 浏览器判定「真实用户刚操作过」的内部标记，剪贴板写入等受限能力只在激活窗口内放行。合成事件通常拿不到它，这类动作在自动化里必然失败。 | headless 下复制按钮恒被拒，于是复制、中文输入搜索、看板拖拽三处证据改由人工录屏承担，而不是在自动化里补一个假绿。 |

## 精选提示词（20 条，逐条待校）

| # | title | useAs | 平台标记 | 变量 | 字节 | 意图（待校：指令是否可执行、有无空话） |
| --- | --- | --- | --- | --- | --- | --- |
| 1 | 规则遵守强化（防漂移） | rule | claude-code cursor generic codebuddy trae minicode | — | 715 | 每轮开工前对规则文件做静默自检，防止代理悄悄扩大改动范围或违反硬性条款。 |
| 2 | 错误处理范式 | rule | generic claude-code | — | 637 | 统一「何时校验、如何吞错、错误信息写什么」三件事，避免防御性代码到处增殖。 |
| 3 | 安全底线（写文件与不可逆操作） | rule | claude-code cursor | — | 756 | 给会改用户文件的代理划出硬红线：路径逃逸、破坏性命令、凭据泄露与人工确认边界。 |
| 4 | 提交信息规范 | rule | generic cursor | — | 435 | 让提交历史可检索、可回滚：一个提交一件事，正文写清为什么。 |
| 5 | SQL 生成约束 | rule | claude-code generic cursor | — | 495 | 生成或改写 SQL 时的硬约束：参数化、写操作先干跑、迁移只向前。 |
| 6 | 正则编写规范 | rule | generic | — | 528 | 防止「看着对、跑起来漏」的正则：先给样例再写式，跑一遍才算完。 |
| 7 | 日志与可观测性 | rule | cursor generic | — | 509 | 让线上一次报错能定位到唯一模块：上下文标识齐全、不泄露隐私、热路径不刷量。 |
| 8 | 代码审查请求 | reference | claude-code cursor generic | diff_range, risk_areas, focus | 783 | 把一段改动交给代理做三维审查（逻辑一致性/批判性/第一性原理），要求先说风险再给改法。 |
| 9 | 重构引导 | reference | claude-code cursor | target_module, compat_constraint, definition_of_done, definition_of_done | 670 | 先要方案后动刀：保持外部行为不变，每步可验证、可回滚。 |
| 10 | 测试生成 | reference | cursor generic windsurf | subject, framework, edge_cases | 639 | 针对一个被测对象生成有区分度的用例，强调「先证失败再证通过」。 |
| 11 | Bug 最小复现 | reference | claude-code minicode | error_text, environment, recent_change | 693 | 把一个偶发缺陷收敛到最小可重放路径，先取证据再改代码。 |
| 12 | 上下文装载（接手项目） | reference | claude-code cursor generic codebuddy | project_name, stack, entry_points | 714 | 进陌生仓库时先建立地图再动手：结构、入口、约定、禁区一次摸清。 |
| 13 | 文档抽取 | reference | generic trae | source_files, audience | 595 | 从既有代码与配置里反向抽出可信文档，明确区分事实与推断。 |
| 14 | API 设计评审 | reference | claude-code generic | contract, constraints, consumers | 775 | 评审一份接口契约：向后兼容、错误语义、幂等与鉴权边界逐项过。 |
| 15 | 类型补全 | reference | cursor windsurf generic | snippet, ts_strictness | 576 | 在不放宽类型的前提下消除 any 与断言，让类型系统真正承担约束。 |
| 16 | 性能剖析引导 | reference | claude-code trae | suspect_area, budget, measurement | 711 | 先量化再优化：定位热点、给出预算与可证伪的改进假设。 |
| 17 | 依赖升级评估 | reference | cursor codex generic | package, from_version, to_version | 638 | 升级一个依赖前先看清破坏面、维护状况与回滚路径。 |
| 18 | 前端组件规格 | reference | cursor trae codebuddy | component, interaction, state_model | 797 | 写组件规格时穷举状态与交互细节，避免实现阶段反复回问。 |
| 19 | 数据建模引导 | reference | generic claude-code | domain_entities, query_patterns, retention | 818 | 从查询模式倒推表结构，避免为了「以后可能用到」先建三张表。 |
| 20 | 复盘报告生成 | reference | generic minicode trae | period, project, metrics | 728 | 一段时间结束时按维度归类问题、给出定级与可执行行动项。 |

## 裁决记录（owner 填写）

| 条目 | 结论（留 / 改 / 删） | 备注 |
| --- | --- | --- |
| （待填） | | |

