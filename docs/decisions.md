# 待决策项队列（单源）

**本文件是队列的唯一事实源。** 此前 ①–⑯ 只活在 `DEV_LOG.md` 每条末尾的散文行里，每轮重建要靠 `grep`，且已两次数错（把 ⑭ 的归因写成 C-88、把队列项数数成 14/9 两种口径）。自 2026-09-26 起：

- **更新规则**：任何一项的状态变化，先改本文件对应行；`DEV_LOG` 登记里只写「队列见 `docs/decisions.md`」+ 本笔动了哪一行，**不再复述整份队列**。
- **§13-8 同族限定**：本表的「现状」列允许带数字，但必须带**采样日期**；无日期的数字视为可能已腐烂。
- **编号警告**：①–⑨ 的语义在 `[DEV-0026]` 之前重排过一次——旧 ⑤「三条冒烟接上骨架」已落地出队，后续项前移。**只信本表，不信早期登记里的编号。**

口径：`状态` 只有四种——**待裁**（owner 要选方向）/ **待签**（方向已定，等逐条签字）/ **可做待排**（无需裁决，缺的是排期或人）/ **已裁已落**（关闭，附 `[DEV-NNNN]`）。

| # | 事项 | 状态 | 现状与裁决（实测） | 落点 | 它阻塞什么 |
|---|---|---|---|---|---|
| ① | 种子内容审校裁决 | **待签** | 已按 owner 裁定「预筛短名单」交付：19 行（建议改 10 / 建议留 9），每条带现文·问题·建议改法·凭据。2026-09-26 13:48 现测**结论列 19 行全空 = 0 已签**。其余 47 条按「机器预筛 + owner 抽查」处理，不逐条签 | `docs/devlog-evidence/DEV-0037/seed-prescreen.md` 第 4 节；判据 `…/seed-prescreen.mjs`（可复算，基线 `6c33b50`） | `content/seed/` 的任何改动。种子内容属 C 级，但 `C-87` 记有词条与 default 包指纹耦合 ⇒ 改内容会动 golden，**改前须实测差异面并跑五闸** |
| ② | 真 `npm publish` | **可做待排**（owner 手上） | 未发。发布链的实认证据目前为 0——只有 `bundle:check` 的入口 293.07 kB / 23 chunk 最大 347.45 kB 与本地 `pack` 产物 | `docs/tasks.md` 发布段 | 「装包产物才算数」这条发布口径；`docs/tasks.md` 的发布传播清单（渠道帖 / demo GIF / awesome PR）前置即本条 |
| ③ | 推 tag `v0.1.0` | **可做待排**（owner 手上） | 本地 tag 存在；2026-09-26 13:48 现测**落后 58 笔**（`git rev-list --count v0.1.0..HEAD`）。`git ls-remote --tags origin` **无此 tag** ⇒ 它从未上过远端 | 同 ② | 远端可安装的版本锚点；② 之后才有意义 |
| ④ | D15 两周观察窗 | **待裁** | 无起点。要 owner 定「从哪一天算」：推 tag 日 / 真发布日 / 本笔审计日 | `docs/specs/m5-project-flow.md` 版本行的条件项、`docs/specs/onboarding.md` 证据口径 | dogfooding 证伪线的判定；窗未开 ⇒ 该条件项长期挂空 |
| ⑤ | `design.md` 文档头版本口径 | **待裁** | 文档头仍写 `状态 = 草案`、`版本 = v0.1（2026-09-20）`，而 §7/§8 契约实际已 v1.x 并随 P0 门禁 G2/G3 冻结。要裁的是**治理问题**：谁有权把「草案」改「已冻结」、契约版与文档版如何并存 | `docs/design.md` 文档头；起因登记 `[DEV-0028]` 的「新撞出」条 | 文档级一致性。属 C 级，但 owner 判定它需要授权形状，故不就地改 |
| ⑥ | P1.1 两份 B 级规格一次性复核 | **待裁**（机制已定，复核未做） | owner 已裁「B 级规格改动收口时一次性复核」。复核面 = `m6-cli-injection.md`、`m6-standard-pack.md` 自 v1.0 起的全部 B 级改动（含 ⑫ 的 `playbookIds` 422、⑯ 的 `sync.lock` 读者改法、`--file`/`--dir` 离线入口、FR-6 体量估算）。**注**：spec 版本号单源在各自文件头部 `\| 版本 \|` 行，本表不抄 | `docs/dev-plan.md` §15.1 的 T10 两格（该两格已按 `[DEV-0038]` 去掉写死的版本号） | 收口动作本身。期间「再出现有新行为后果的裁定仍须当场停下问」这条 owner 限定仍生效 |
| ⑦ | 提交与推送 | **部分授权** | 提交：已授权（先文档、再分支逐任务 commit，**不 push**）。推送：2026-09-24 那次「推 main 触发 CI」的授权**只覆盖 `5a963ff` 一笔**。2026-09-26 13:48 现测：真远端 `main` = `5a963ff`，`5a963ff..HEAD` = **11 笔**，CI 未验证 commit = 11 | 队列状态行见本表；推送凭据在 `[DEV-0032]` | tip sha 的三平台 CI 证据；本机 449 支 vs CI 438 支那 **11 支差额**全部落在这 11 笔里 |
| ⑧ | DEV-0025 驱动器接 CI 的有头/xvfb 口径 | **待裁** | 驱动器已入库（`docs/devlog-evidence/DEV-0024/s2-cdp-real-input.mjs`，自带清理断言与负向对照）。卡点是**口径**：剪贴板腿只在**有头**可证，无头 CI 只能带 SKIP ⇒ 接进 CI 等于往三平台报告里加一批「合法但永远不证」的 skip | `[DEV-0025]`、`docs/dev-plan.md` §15.4b（有头 FAIL=0 SKIP=0；无头 FAIL=0 SKIP=2） | 三条冒烟能否成为每次 push 的常驻闸 |
| ⑨ | toast 与弹窗「关闭」同名（a11y） | **待裁** | 实测踩到：二者同名，脚本必须按 `[role="dialog"]` 限定才拿得对按钮。要么改可访问性名字（用户可见面），要么把「查询限定」写成驱动器的固定约定 | 浏览器证据核验记录 | 驱动器与未来 E2E 的稳定性；用户侧无感 |
| ⑩ | `dev-plan` 那批未勾框的语义 | **已裁已落** | `[DEV-0028]`：不补勾、不删除，改为在文件头声明该框不追踪执行，执行台账单源交回 `docs/tasks.md` | `[DEV-0028]` | — |
| ⑪ | `apps/web` 测试基建 | **已裁已落** | `[DEV-0033]`：建 jsdom project（vitest 第 4 个 project）+ 6 支 DOM 断言打在 T8 那四处渲染上，六次变异反证全部命中。续集（扩到 Wizard / Kanban / TermsTable）是可选投入，不催 | `[DEV-0033]` | — |
| ⑫ | `playbookIds` 的契约处理 | **已裁已落** | `[DEV-0034]`：非空即 **422**（收紧为「冲突才拒」的另议项已并入 lock `files[].path` 闸）。同笔公开纠正了我给 owner 的选项文本里误写的 400 | `[DEV-0034]` | — |
| ⑬ | 一次 push（证 win32 skip 4→8） | **已裁已落** | `[DEV-0032]`：run `36159308661` @ `5a963ff` 三平台 `success`；**macOS 438 passed / 0 skip · ubuntu 438 / 0 · win32 430 passed + 8 skipped**，`Test Files` 三侧恒 47。预测立在拿数之前且全部命中 | `[DEV-0032]` | — |
| ⑭ | 遥测适配器 timeout 路径下的泄漏清扫 | **已裁已落** | `[DEV-0031]`：根因不是「`afterEach` 没跑」，是**拒签路径根本不收孩子**；一次性双跑探针给出差集 | `[DEV-0031]` | — |
| ⑮ | ⑭ 修复的常驻断言要不要补 | **已裁已落** | `[DEV-0036]`：补 TF-14。动手前实测——删掉那行 `proc.kill()` 曾让全文件 14 支仍绿，现在只有 TF-14 会红 | `[DEV-0036]` | — |
| ⑯ | `sync.lock` 的 0 字节空窗竞态 | **已裁已落** | `[DEV-0035]`：**修的是读者不是写者**；让路方从「约 7% 波次报不出赢家 pid」变成「100 波 0 异常」 | `[DEV-0035]` | — |

## 净状态（2026-09-26 13:48 现测）

- **待裁 5**：④ ⑤ ⑥ ⑧ ⑨
- **待签 1**：①
- **可做待排（owner 手上，不需裁决）2**：② ③
- **部分授权 1**：⑦（提交已授权；推送需重新放行）
- **已裁已落 7**：⑩ ⑪ ⑫ ⑬ ⑭ ⑮ ⑯

`①–⑯` 之外没有隐藏项：本轮体检新增的两件事（三处文档状态腐烂、本队列文件）属 C 级就地动作，已随 `[DEV-0038]` 落盘，不入队列。
