// apps/web —— React SPA（/library /terms /skills /flows /projects /packs /settings）。
// Vite + React + TanStack Query 脚手架随 T3 落地（dev-plan §5）；当前仅固定包边界（R4：与 server 只经 HTTP）。
export const WEB_VERSION = '0.0.0'
